target
**/*.rs.bk
.vscode
.idea
/.direnv/bin
/.direnv
/scripts/**/__pycache__/
/contracts/bootstate/model/.trace-query-*/
# Every seL4 product build output: the kernel build tree and install prefix,
# the per-artifact cargo target dirs, the packaged image, and its identity
# manifest all land under /build/.
/build/
