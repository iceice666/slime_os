-- Slime OS relocatable kernel image format, version 2 (P0).
--
-- Version 2 adds the target-qualification fields version 1 left implicit:
-- `architecture`, the architecture-qualified `abi`, `page_profile`, and the
-- exact `required_features` mask. Segment and relocation records are unchanged.
--
-- Version 1 carried `kernel_abi` and a `required_flags` word, but neither
-- identified a machine: a v1 kernel image is x86-64 because the only builder
-- that could emit one targeted x86-64, not because it says so. Readers retain
-- v1 for the bounded rollback window and give it exactly that legacy meaning --
-- `x86_64-qemu-virtio` -- rather than treating it as architecture-neutral. The
-- distinct `SLIMEKR2` magic makes that a decode-time fact.
--
-- `preferred_base`/`load_base` remain in the header rather than being implied
-- by the profile: they are properties of how this image was linked, and the
-- decoder cross-checks them against the profile's declared bases so a
-- correctly-tagged image linked for the wrong layout still fails closed.

env ::= import stdlib.env;
fs ::= import stdlib.fs;
gen ::= import "gen_rust.zt";
t ::= import stdlib.text;

formatVersion :: Int = 2;
kernelAbiVersion :: Int = 2;
headerLen :: Int = 88;
segmentLen :: Int = 40;
relocationLen :: Int = 16;
maxImageBytes :: Int = 8388608;
maxSegments :: Int = 8;
maxRelocations :: Int = 16384;
preferredBaseText :: Text = "0xFFFF_FFFF_8000_0000";
loadBaseText :: Text = "0xFFFF_FFFF_9000_0000";
segmentWrite :: Int = 1;
segmentExec :: Int = 2;
legacyFormatVersion :: Int = 1;
legacyHeaderLen :: Int = 64;

WireField :: type { name : Text; width : Int; signed : Bool; byteArray : Bool; };

KernelImageHeader :: type {
  magic : Int;
  format_version : Int;
  header_size : Int;
  kernel_abi : Int;
  required_flags : Int;
  architecture : Int;
  abi : Int;
  page_profile : Int;
  target_profile : Int;
  required_features : Int;
  preferred_base : Int;
  entry_offset : Int;
  segment_count : Int;
  relocation_count : Int;
  payload_offset : Int;
  total_len : Int;
};

KernelSegmentRecord :: type {
  vaddr_offset : Int;
  mem_len : Int;
  file_offset : Int;
  file_len : Int;
  flags : Int;
  reserved : Int;
};

KernelRelocationRecord :: type {
  target_offset : Int;
  addend : Int;
};

kernelImageHeaderSchema ::= schema KernelImageHeader;
kernelSegmentRecordSchema ::= schema KernelSegmentRecord;
kernelRelocationRecordSchema ::= schema KernelRelocationRecord;

headerLayout :: List WireField = {
  { name = "magic"; width = 8; signed = false; byteArray = true; };
  { name = "format_version"; width = 4; signed = false; byteArray = false; };
  { name = "header_size"; width = 4; signed = false; byteArray = false; };
  { name = "kernel_abi"; width = 4; signed = false; byteArray = false; };
  { name = "required_flags"; width = 4; signed = false; byteArray = false; };
  { name = "architecture"; width = 4; signed = false; byteArray = false; };
  { name = "abi"; width = 4; signed = false; byteArray = false; };
  { name = "page_profile"; width = 4; signed = false; byteArray = false; };
  { name = "target_profile"; width = 4; signed = false; byteArray = false; };
  { name = "required_features"; width = 8; signed = false; byteArray = false; };
  { name = "preferred_base"; width = 8; signed = false; byteArray = false; };
  { name = "entry_offset"; width = 8; signed = false; byteArray = false; };
  { name = "segment_count"; width = 4; signed = false; byteArray = false; };
  { name = "relocation_count"; width = 4; signed = false; byteArray = false; };
  { name = "payload_offset"; width = 8; signed = false; byteArray = false; };
  { name = "total_len"; width = 8; signed = false; byteArray = false; };
};

segmentLayout :: List WireField = {
  { name = "vaddr_offset"; width = 8; signed = false; byteArray = false; };
  { name = "mem_len"; width = 8; signed = false; byteArray = false; };
  { name = "file_offset"; width = 8; signed = false; byteArray = false; };
  { name = "file_len"; width = 8; signed = false; byteArray = false; };
  { name = "flags"; width = 4; signed = false; byteArray = false; };
  { name = "reserved"; width = 4; signed = false; byteArray = false; };
};

relocationLayout :: List WireField = {
  { name = "target_offset"; width = 8; signed = false; byteArray = false; };
  { name = "addend"; width = 8; signed = true; byteArray = false; };
};

format ::= {
  formatVersion =; kernelAbiVersion =;
  headerLen =; segmentLen =; relocationLen =;
  maxImageBytes =; maxSegments =; maxRelocations =;
  preferredBaseText =; loadBaseText =;
  segmentWrite =; segmentExec =;
  legacyFormatVersion =; legacyHeaderLen =;
  headerFields = kernelImageHeaderSchema.fields ?? {;};
  segmentFields = kernelSegmentRecordSchema.fields ?? {;};
  relocationFields = kernelRelocationRecordSchema.fields ?? {;};
  headerLayout =; segmentLayout =; relocationLayout =;
};
main :: { write : FsWrite; env : Env; } -> Unit ! { * fs.WholeWriteEffects; * env.GetEffects; }
  = caps => [
    root := env.get caps.env "SLIME_BOOT_BINDINGS_ROOT" ?? ".";
    bindings := gen.render format;
    pythonPath := t.join "" { root; "/kernel_image.py"; };
    rustPath := t.join "" { root; "/kernel_image.rs"; };
    fs.writeAll caps.write pythonPath bindings.python;
    fs.writeAll caps.write rustPath bindings.rust
  ];

main
