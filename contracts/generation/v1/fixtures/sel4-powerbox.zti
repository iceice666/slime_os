{
  formatVersion = 1;
  generation = 32;
  target = "aarch64-sel4-qemu-virt";
  kernelObject = "sha256:kernel";
  bootstrapComponent = "init";

  interfaceSchemas = [];

  objects = [
    { id = "sha256:kernel"; kind = "kernel"; size = 1048576; };
    { id = "sha256:init"; kind = "bootstrap"; size = 65536; };
    { id = "sha256:powerbox-chooser"; kind = "component"; size = 65536; };
    { id = "sha256:powerbox-probe"; kind = "component"; size = 65536; };
    { id = "boot-layout"; kind = "resource"; size = 4096; };
  ];

  sharedBufferBudget = [];

  components = [
    {
      name = "init";
      object = "sha256:init";
      role = "init";
      dependencies = [];
      spawnBudget = 2;
      commandProfile = [];
    };
    {
      name = "powerbox-chooser";
      object = "sha256:powerbox-chooser";
      role = "service";
      dependencies = ["init";];
      spawnBudget = 0;
      commandProfile = [];
    };
    {
      name = "powerbox-probe";
      object = "sha256:powerbox-probe";
      role = "application";
      dependencies = ["init";];
      spawnBudget = 0;
      commandProfile = [];
    };
  ];

  grants = [
    {
      name = "init-powerbox-chooser";
      source = "init";
      target = "powerbox-chooser";
      rights = ["exec"; "spawn";];
      transferable = false;
    };
    {
      name = "init-powerbox-probe";
      source = "init";
      target = "powerbox-probe";
      rights = ["exec"; "spawn";];
      transferable = false;
    };
    {
      name = "powerbox-a-root";
      source = "powerbox-chooser";
      target = "powerbox-chooser";
      rights = ["directoryRead"; "directoryDerive";];
      transferable = true;
    };
    {
      name = "powerbox-b-input";
      source = "powerbox-chooser";
      target = "powerbox-chooser";
      rights = ["inputRead";];
      transferable = false;
    };
    {
      name = "init-endpoint-factory";
      source = "init";
      target = "init";
      rights = ["endpointCreate";];
      transferable = false;
    };
  ];

  state = [];

  health = {
    bootAttempts = 3;
    requiredComponents = ["init";];
  };
}
