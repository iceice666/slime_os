{
  formatVersion = 1;
  generation = 28;
  target = "aarch64-sel4-qemu-virt";
  kernelObject = "sha256:kernel";
  bootstrapComponent = "init";

  interfaceSchemas = [];

  objects = [
    { id = "sha256:kernel"; kind = "kernel"; size = 1048576; };
    { id = "sha256:init"; kind = "bootstrap"; size = 65536; };
    { id = "sha256:sel4-directory-probe"; kind = "component"; size = 65536; };
    { id = "boot-layout"; kind = "resource"; size = 4096; };
  ];

  sharedBufferBudget = [];

  components = [
    {
      name = "init";
      object = "sha256:init";
      role = "init";
      dependencies = [];
      spawnBudget = 1;
      commandProfile = [];
    };
    {
      name = "sel4-directory-probe";
      object = "sha256:sel4-directory-probe";
      role = "application";
      dependencies = ["init";];
      spawnBudget = 0;
      commandProfile = [];
    };
  ];

  grants = [
    {
      name = "init-sel4-directory-probe";
      source = "init";
      target = "sel4-directory-probe";
      rights = ["exec"; "spawn";];
      transferable = false;
    };
    {
      name = "directory-probe-root";
      source = "sel4-directory-probe";
      target = "sel4-directory-probe";
      rights = ["directoryRead"; "directoryWrite"; "directoryList"; "directoryDerive";];
      transferable = false;
    };
    {
      name = "init-endpoint-factory";
      source = "init";
      target = "init";
      rights = ["endpointCreate";];
      transferable = false;
    };
  ];

  state = [];

  health = {
    bootAttempts = 3;
    requiredComponents = ["init";];
  };
}
