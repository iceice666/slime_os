{
  formatVersion = 1;
  generation = 29;
  target = "aarch64-sel4-qemu-virt";
  kernelObject = "sha256:kernel";
  bootstrapComponent = "init";

  interfaceSchemas = [];

  objects = [
    { id = "sha256:kernel"; kind = "kernel"; size = 1048576; };
    { id = "sha256:init"; kind = "bootstrap"; size = 65536; };
    { id = "sha256:sel4-filesystem-service"; kind = "component"; size = 131072; };
    { id = "sha256:directory-probe"; kind = "component"; size = 65536; };
    { id = "boot-layout"; kind = "resource"; size = 4096; };
  ];

  sharedBufferBudget = [];

  components = [
    {
      name = "init";
      object = "sha256:init";
      role = "init";
      dependencies = [];
      spawnBudget = 2;
      commandProfile = [];
    };
    {
      name = "sel4-filesystem-service";
      object = "sha256:sel4-filesystem-service";
      role = "service";
      dependencies = ["init";];
      spawnBudget = 0;
      commandProfile = [];
    };
    {
      name = "directory-probe";
      object = "sha256:directory-probe";
      role = "application";
      dependencies = ["init";];
      spawnBudget = 0;
      commandProfile = [];
    };
  ];

  grants = [
    {
      name = "init-sel4-filesystem-service";
      source = "init";
      target = "sel4-filesystem-service";
      rights = ["exec"; "spawn";];
      transferable = false;
    };
    {
      name = "init-directory-probe";
      source = "init";
      target = "directory-probe";
      rights = ["exec"; "spawn";];
      transferable = false;
    };
    {
      name = "filesystem-service-a-namespace";
      source = "sel4-filesystem-service";
      target = "sel4-filesystem-service";
      rights = ["directoryRead"; "directoryWrite"; "directoryList"; "directoryDerive";];
      transferable = false;
    };
    {
      name = "filesystem-service-b-device";
      source = "sel4-filesystem-service";
      target = "sel4-filesystem-service";
      rights = ["blockRead"; "blockWrite";];
      transferable = false;
    };
    {
      name = "directory-probe-namespace";
      source = "directory-probe";
      target = "directory-probe";
      rights = ["directoryRead"; "directoryWrite"; "directoryList"; "directoryDerive";];
      transferable = true;
    };
    {
      name = "init-endpoint-factory";
      source = "init";
      target = "init";
      rights = ["endpointCreate";];
      transferable = false;
    };
  ];

  state = [];

  health = {
    bootAttempts = 3;
    requiredComponents = ["init";];
  };
}
