BootStateBinding :: type { formatVersion : Int; slotBytes : Int; slotCount : Int; };
render :: BootStateBinding -> Text
  = format => if format.formatVersion == 1 && format.slotBytes == 512 && format.slotCount == 2
    then "bootstate-v1"
    else "INVALID_BOOTSTATE_SCHEMA";
{ render = render; }
