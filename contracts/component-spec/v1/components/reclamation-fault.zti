{
  formatVersion = 1;
  name = "reclamation-fault";
  componentType = "application";
  version = "1.0.0";
  owner = "init";
  purpose = "Deliberately faults through a null pointer to exercise the supervised VM-fault reclamation path repeatedly, past the old CSlot/untyped watermark.";
  implementation = {
    provider = "workspace";
    binary = "reclamation-fault";
    contentHash = "";
  };
  provides = [];
  requires = [];
  interfaces = [];
  dependencies = [
    "init";
  ];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_reclamation_check gate boots a generation declaring reclamation-fault";
    expectedResult = "the deliberate fault is supervised, reclaimed, and reused past the old watermark";
    passFailCriteria = "[init] reclamation fault path reused";
    requiredTestEnvironment = "sel4_reclamation_check";
  };
}
