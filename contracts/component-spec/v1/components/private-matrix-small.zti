{
  communication = {
    qos = [];
    semantic = "none";
  };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = [
    "private-matrix-coordinator";
  ];
  formatVersion = 1;
  health = "optional";
  implementation = {
    binary = "private-matrix-small";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  name = "private-matrix-small";
  owner = "private-matrix-coordinator";
  provides = [];
  purpose = "A pool-relative subject whose requests never cover an aligned 2 MiB span, so every page it holds is a base page.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 0;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "Its walk ends at an exact one-page refusal and reports the resource that limited it.";
    passFailCriteria = "[private-matrix:small] end incarnation=";
    requiredTestEnvironment = "private_memory_matrix_check";
    testCondition = "Spawned by the matrix coordinator against each pinned inventory row.";
  };
  version = "1.0.0";
}
