{
  formatVersion = 1;
  name = "sel4-recovery-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "init";
  purpose = "Drives the recovery path over the userspace block driver: a failing pending generation is detected and the known-good root is restored.";
  implementation = {
    provider = "workspace";
    binary = "sel4-recovery-probe";
    contentHash = "";
  };
  provides = [
    "endpoint";
  ];
  requires = [
    "endpoint";
    "sharedBufferFactory";
  ];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_recovery_plane_check gate boots a generation declaring sel4-recovery-probe";
    expectedResult = "a failed generation recovers to its verified known-good root across fresh boots";
    passFailCriteria = "[init] recovery probe spawned";
    requiredTestEnvironment = "sel4_recovery_plane_check";
  };
}
