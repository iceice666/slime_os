{
  formatVersion = 1;
  name = "fabric-op-time";
  componentType = "service";
  version = "1.0.0";
  owner = "root";
  purpose = "Advances the operation plane's monotonic clock one step per client phase signal, then exits.";
  implementation = {
    provider = "workspace";
    binary = "fabric-op-time";
    contentHash = "";
  };
  provides = [
    "endpoint";
  ];
  requires = [];
  interfaces = [];
  dependencies = [
    "fabric-service";
  ];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Ready";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_operation_check gate boots a generation declaring fabric-op-time";
    expectedResult = "fabric-op-time reaches its declared behavior under that gate";
    passFailCriteria = "[fabric-op-time] bounded time advanced";
    requiredTestEnvironment = "sel4_operation_check";
  };
}
