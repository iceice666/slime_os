{
  formatVersion = 1;
  name = "io-driver-intruder";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Proves an ungranted component receives structured denial for every hardware-resource operation.";
  implementation = { provider = "workspace"; binary = "io-driver-intruder"; contentHash = ""; };
  provides = [];
  requires = [];
  interfaces = [];
  dependencies = [];
  communication = { semantic = "none"; qos = []; };
  configuration = [];
  lifecycle = [ "Initialize"; "Start"; "Running"; "Error"; ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = { stackBytes = 16384; spawnBudget = 0; extraThreads = 0; bufferBytePages = 0; bufferCount = 0; mappingCount = 0; loanCount = 0; privatePageQuota = 0; };
    devices = [];
  };
  health = "required";
  compatibility = { platform = "aarch64-sel4-qemu-virt"; interface = "contracts/interface-schema/v1"; dependency = "none"; resource = "atMost"; runtime = "exact"; qos = "none"; };
  test = { testCondition = "the io_driver_authority_check gate boots the authority composition"; expectedResult = "every attempted device, MMIO, DMA, and interrupt operation is denied without a fault"; passFailCriteria = "[io-driver-intruder] denied device=1 mmio=2 dma=2 interrupt=1"; requiredTestEnvironment = "io_driver_authority_check"; };
}
