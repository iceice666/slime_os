{
  formatVersion = 1;
  name = "fabric-publisher";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Requests a publish-only telemetry role, asserts forbidden operations are denied, and publishes inline samples.";
  implementation = {
    provider = "workspace";
    binary = "fabric-publisher";
  };
  provides = [
    "endpoint";
  ];
  requires = [];
  interfaces = [
    {
      name = "telemetry";
      tag = "output";
      interface = "TelemetryStream";
    };
  ];
  dependencies = [
    "fabric-service";
  ];
  communication = {
    semantic = "stream";
    qos = [
      {
        reference = "telemetry";
        reliability = "bestEffort";
        durability = "retained";
        liveliness = "automatic";
        historyDepth = 4;
        retainedDepth = 2;
        deadlineNs = 0;
        lifespanNs = 300;
        leaseNs = 0;
      };
    ];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Degraded";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "compatible";
  };
  test = {
    testCondition = "the sel4_stream_check gate boots a generation declaring fabric-publisher";
    expectedResult = "fabric-publisher reaches its declared behavior under that gate";
    passFailCriteria = "[fabric-publisher] done";
    requiredTestEnvironment = "sel4_stream_check";
  };
}
