{
  formatVersion = 1;
  name = "sel4-input-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "init";
  purpose = "Reads from a granted input capability and proves an instance without that grant reaches no input at all.";
  implementation = {
    provider = "workspace";
    binary = "sel4-input-probe";
    contentHash = "";
  };
  provides = [
    "endpoint";
    "input";
  ];
  requires = [
    "endpoint";
    "input";
  ];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_input_check gate boots a generation declaring sel4-input-probe";
    expectedResult = "the granted holder reads its declared input source and an ungranted one is refused";
    passFailCriteria = "[init] input plane complete";
    requiredTestEnvironment = "sel4_input_check";
  };
}
