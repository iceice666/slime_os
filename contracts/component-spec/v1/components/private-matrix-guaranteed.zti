{
  communication = {
    qos = [];
    semantic = "none";
  };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = [
    "private-matrix-coordinator";
  ];
  formatVersion = 1;
  health = "optional";
  implementation = {
    binary = "private-matrix-guaranteed";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  name = "private-matrix-guaranteed";
  owner = "private-matrix-coordinator";
  provides = [];
  purpose = "A subject with a 1024-page guarantee and a 2048-page maximum, redeemed while the pool is exhausted.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 0;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "Its guarantee is served under pressure and its first page past the promise is refused.";
    passFailCriteria = "[private-matrix:guaranteed] end incarnation=";
    requiredTestEnvironment = "private_memory_matrix_check";
    testCondition = "Spawned by the matrix coordinator against each pinned inventory row.";
  };
  version = "1.0.0";
}
