{
  formatVersion = 1;
  name = "fabric-op-server";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Runs the operation plane's server role, streaming feedback and honouring rejection and cancellation.";
  implementation = {
    provider = "workspace";
    binary = "fabric-op-server";
    contentHash = "";
  };
  provides = [
    "endpoint";
  ];
  requires = [];
  interfaces = [
    {
      name = "navigation";
      tag = "event";
      interface = "NavigationOperation";
    };
  ];
  dependencies = [
    "fabric-service";
  ];
  communication = {
    semantic = "operation";
    qos = [
      {
        reference = "navigation";
        reliability = "reliable";
        durability = "volatile";
        liveliness = "manual";
        historyDepth = 4;
        retainedDepth = 0;
        deadlineNs = 1000000;
        lifespanNs = 2000000;
        leaseNs = 5000000;
      };
    ];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Degraded";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "compatible";
  };
  test = {
    testCondition = "the sel4_operation_check gate boots a generation declaring fabric-op-server";
    expectedResult = "fabric-op-server reaches its declared behavior under that gate";
    passFailCriteria = "[fabric-op-server] feedback streamed";
    requiredTestEnvironment = "sel4_operation_check";
  };
}
