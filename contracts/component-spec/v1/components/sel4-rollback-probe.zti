{
  formatVersion = 1;
  name = "sel4-rollback-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "init";
  purpose = "Stages a generation, selects it, observes the failure, and rolls back to the previous verified generation over the declared block rings.";
  implementation = {
    provider = "workspace";
    binary = "sel4-rollback-probe";
    contentHash = "";
  };
  provides = [
    "endpoint";
  ];
  requires = [
    "endpoint";
    "sharedBufferFactory";
  ];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_rollback_check gate boots a generation declaring sel4-rollback-probe";
    expectedResult = "rollback restores the previous generation and its boot state is persisted";
    passFailCriteria = "[init] rollback probe spawned";
    requiredTestEnvironment = "sel4_rollback_check";
  };
}
