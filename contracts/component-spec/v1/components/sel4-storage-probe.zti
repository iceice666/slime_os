{
  formatVersion = 1;
  name = "sel4-storage-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "init";
  purpose = "Reads and writes sectors through its declared per-ring block authority and proves a request beyond the declared sector ceiling is refused.";
  implementation = {
    provider = "workspace";
    binary = "sel4-storage-probe";
    contentHash = "";
  };
  provides = [
    "endpoint";
  ];
  requires = [
    "endpoint";
    "sharedBufferFactory";
  ];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_storage_check gate boots a generation declaring sel4-storage-probe";
    expectedResult = "declared reads and writes complete and an out-of-bound request is refused";
    passFailCriteria = "[init] storage plane complete";
    requiredTestEnvironment = "sel4_storage_check";
  };
}
