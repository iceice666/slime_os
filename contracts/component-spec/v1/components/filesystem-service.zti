{
  formatVersion = 1;
  name = "filesystem-service";
  componentType = "service";
  version = "1.0.0";
  owner = "root";
  purpose = "Resolves paths in a snapshot tree and reads, writes, and derives directory objects over a userspace virtio block driver.";
  implementation = {
    provider = "workspace";
    binary = "sel4-filesystem-service";
    contentHash = "";
  };
  provides = [];
  requires = [
    "endpoint";
  ];
  interfaces = [];
  dependencies = [
    "init";
  ];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Ready";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the sel4_filesystem_check gate boots a generation declaring filesystem-service";
    expectedResult = "filesystem-service reaches its declared behavior under that gate";
    passFailCriteria = "[init] filesystem plane complete";
    requiredTestEnvironment = "sel4_filesystem_check";
  };
}
