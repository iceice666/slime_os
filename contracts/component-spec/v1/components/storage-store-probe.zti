{
  formatVersion = 1;
  name = "storage-store-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Declared object-store probe; no component binary exists for this name in this repository.";
  implementation = {
    provider = "undeclared";
    binary = "";
    contentHash = "";
  };
  provides = [];
  requires = [];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
    };
    devices = [];
  };
  health = "optional";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the generation_check gate validates the manifest declaring storage-store-probe";
    expectedResult = "storage-store-probe is declared but no implementation is admitted, so no behavior is claimed";
    passFailCriteria = "generation determinism check";
    requiredTestEnvironment = "generation_check";
  };
}
