{
  communication = { qos = []; semantic = "none"; };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = ["private-matrix-coordinator";];
  formatVersion = 1;
  health = "optional";
  implementation = {
    binary = "private-matrix-probe";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = ["Initialize"; "Start"; "Running"; "Error";];
  name = "private-matrix-probe";
  owner = "private-matrix-coordinator";
  provides = [];
  purpose = "Construct and execute without private-memory authority while both mixed-pressure holders remain resident.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 0;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "Construction succeeds under mixed exhaustion, private growth is denied, and clean exit permits reclamation.";
    passFailCriteria = "[private-matrix:probe] ran pages=0 base=0x0 refused=1";
    requiredTestEnvironment = "private_memory_matrix_check";
    testCondition = "Spawned after both mixed holders exhaust and before either is released.";
  };
  version = "1.0.0";
}
