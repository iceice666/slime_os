{
  formatVersion = 1;
  name = "io-network-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Proves its exact declared TCP and UDP destinations, exact-name DNS, and per-holder socket and DNS-record budgets, with no ambient network authority.";
  implementation =  {
    provider = "workspace";
    binary = "io-network-probe";
    contentHash = "";
  };
  provides = [
  ];
  requires = [
    "endpoint";
  ];
  interfaces = [
  ];
  dependencies = [
    "network-service";
  ];
  communication =  {
    semantic = "none";
    qos = [
    ];
  };
  configuration = [
  ];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime =  {
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [
    ];
  };
  health = "required";
  compatibility =  {
    platform = "aarch64-sel4-qemu-virt";
    interface = "contracts/interface-schema/v1";
    dependency = "exact";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test =  {
    testCondition = "io_network_check boots sel4-io-network";
    expectedResult = "the granted client reaches only its declared destinations, is refused past its declared budgets, and closes every capability it opened";
    passFailCriteria = "[io-network-probe] closed capabilities=4 shutdown=1";
    requiredTestEnvironment = "io_network_check";
  };
}
