{
  communication = {
    qos = [];
    semantic = "none";
  };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = [
    "init";
  ];
  formatVersion = 1;
  health = "required";
  implementation = {
    binary = "private-matrix-coordinator";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  name = "private-matrix-coordinator";
  owner = "root";
  provides = [];
  purpose = "Drives pool-relative holders to exact exhaustion under three schedules and reports what each holder verified resident.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 5;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "Every schedule ends in a one-page refusal, verified residency grows with the booted inventory, and twenty reuse cycles leave the census unchanged.";
    passFailCriteria = "[private-matrix] complete schedules=3 cycles=";
    requiredTestEnvironment = "private_memory_matrix_check";
    testCondition = "One unchanged image booted against each pinned kernel-visible inventory row.";
  };
  version = "1.0.0";
}
