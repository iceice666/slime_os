{
  communication = {
    qos = [];
    semantic = "none";
  };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = [
    "init";
  ];
  formatVersion = 1;
  health = "required";
  implementation = {
    binary = "private-adaptive-coordinator";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  name = "private-adaptive-coordinator";
  owner = "root";
  provides = [];
  purpose = "Drives the adaptive plane's fixed request schedule and reports every grant and refusal it observed.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 4;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "The same grant and refusal sequence, window bases and digest on two boots of one image.";
    passFailCriteria = "[private-adaptive] complete steps=18 served=";
    requiredTestEnvironment = "private_memory_adaptive_check";
    testCondition = "One coordinator spawns four adaptive holders, repeats a fixed request cycle and drives a fault, a replacement and a clean exit.";
  };
  version = "1.0.0";
}
