{
  communication = {
    qos = [];
    semantic = "none";
  };
  compatibility = {
    dependency = "exact";
    interface = "contracts/interface-schema/v1";
    platform = "aarch64-sel4-qemu-virt";
    qos = "none";
    resource = "atMost";
    runtime = "exact";
  };
  componentType = "application";
  configuration = [];
  dependencies = [
    "init";
  ];
  formatVersion = 1;
  health = "optional";
  implementation = {
    binary = "private-adaptive-pool-b";
    contentHash = "";
    provider = "workspace";
  };
  interfaces = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  name = "private-adaptive-pool-b";
  owner = "root";
  provides = [];
  purpose = "The boot-staged zero-guarantee subject sharing one entitlement with a dynamically spawned peer.";
  requires = [];
  runtime = {
    devices = [];
    executionEnvironment = "aarch64-sel4-qemu-virt";
    resource = {
      bufferBytePages = 0;
      bufferCount = 0;
      extraThreads = 0;
      loanCount = 0;
      mappingCount = 0;
      privatePageQuota = 0;
      spawnBudget = 0;
      stackBytes = 16384;
    };
  };
  test = {
    expectedResult = "A staged subject binds its entitlement before publication and is refused one page past its declared maximum.";
    passFailCriteria = "[private-adaptive:pool-b] end incarnation=0 pages";
    requiredTestEnvironment = "private_memory_adaptive_check";
    testCondition = "The same entitlement is held by a root-owned staged subject and a coordinator-owned spawned subject.";
  };
  version = "1.0.0";
}
