{
  formatVersion = 1;
  name = "storage-probe";
  componentType = "application";
  version = "1.0.0";
  owner = "root";
  purpose = "Declared frozen CP1 storage probe; its implementation retired when seL4 storage moved to the userspace virtio-blk driver.";
  implementation = {
    provider = "undeclared";
    binary = "";
    contentHash = "";
  };
  provides = [];
  requires = [];
  interfaces = [];
  dependencies = [];
  communication = {
    semantic = "none";
    qos = [];
  };
  configuration = [];
  lifecycle = [
    "Initialize";
    "Start";
    "Running";
    "Error";
  ];
  runtime = {
    executionEnvironment = "x86_64-qemu-virtio";
    resource = {
      stackBytes = 16384;
      spawnBudget = 0;
      extraThreads = 0;
      bufferBytePages = 0;
      bufferCount = 0;
      mappingCount = 0;
      loanCount = 0;
      privatePageQuota = 0;
    };
    devices = [];
  };
  health = "required";
  compatibility = {
    platform = "x86_64-qemu-virtio";
    interface = "contracts/interface-schema/v1";
    dependency = "none";
    resource = "atMost";
    runtime = "exact";
    qos = "none";
  };
  test = {
    testCondition = "the generation_check gate validates the frozen reference manifest declaring storage-probe";
    expectedResult = "storage-probe remains declared for fixture compatibility but no implementation is admitted";
    passFailCriteria = "generation determinism check";
    requiredTestEnvironment = "generation_check";
  };
}
