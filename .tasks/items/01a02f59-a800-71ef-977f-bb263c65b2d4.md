---
schema: work-item/v1
id: 01a02f59-a800-71ef-977f-bb263c65b2d4
key: B77
kind: bug
state: done
created: 2026-08-24T00:00:00+08:00
closed: 2026-08-24T00:00:00+08:00
tags:
  - backlog
---

# `budget_us`/`period_us` are authenticated but unvalidated and unread

## Context

Resolved 2026-08-24. **Class:** Debt (authenticated wire fields no mechanism honoured, admitted by both readers and consumed by neither).

A v5 generation could declare a nonzero `budget_us`/`period_us`, pass `check-generation.py` and `Generation::validate`, boot, and be scheduled with no budget at all, because the builder's zero was a convention rather than an admitted invariant and `slime-root` read only `Schedule.priority`.

Class: Debt (authenticated wire fields no mechanism honoured, admitted by both readers and consumed by neither).

## Exit conditions

both readers now refuse with distinct reasons — `UndeclarableCpuBudget` from the host oracle and `DecodeError::NonZeroReserved` from the decoder — and `just generation_check` drives four resealed mutations (both fields, first and last schedule record) through both readers, each guard proven load-bearing by weakening one at a time and observing the gate fail; `just contracts_check`, `just test_sel4_root` (152), and `just sel4_root_boot_check` pass.

## Verification

Gates: `just contracts_check`, `just generation_check`, `just sel4_root_boot_check`, `just test_sel4_root`

## Evidence

[`devlog/2026-08-24-b77-undeclarable-cpu-budget/`](../devlog/2026-08-24-b77-undeclarable-cpu-budget/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 187: `B77 — `budget_us`/`period_us` are authenticated but unvalidated and unread`.
