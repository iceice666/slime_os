---
schema: work-item/v1
id: 01a04919-7400-71d0-99c9-79a20e1974d8
key: B88
kind: bug
state: done
created: 2026-08-29T00:00:00+08:00
closed: 2026-08-29T00:00:00+08:00
tags:
  - backlog
---

# the network-destination decoder read a Zutai-declared layout through hand-written byte literals

## Context

Resolved 2026-08-29. **Class:** Debt (Zutai-rule violation: a schema-declared layout restated as source literals).

`contracts/network-destination/v1/schema.zt` declared complete `headerLayout`/`entryLayout` records and the Python renderer emitted their offsets, but the Rust renderer emitted only scalar constants — so `boot-contracts/src/network_destination.rs` reached all twenty-two fields of IO4's authority record through hard-coded byte offsets (`entry[32]`, `u16_at(entry, 34)`, `entry[56..120]`, eight budget literals), and its own test encoder restated the same literals a second time, leaving no mechanism by which a schema edit could disagree with either copy. The repository rule requires the bindings to be generated from the contract.

Class: Debt (Zutai-rule violation: a schema-declared layout restated as source literals).

## Exit conditions

The Rust renderer now emits `OFF_HEADER_*`/`OFF_ENTRY_*` (plus `_END`) constants and a schema-declared `ipv4Bytes`, and every literal in the decoder and its test encoder was replaced by them; `generate-boot-bindings.py --check` reports bindings current, `boot-contracts` grew from 329 to 335 tests with the destination module going from 3 positive-path tests to 9, four parser mutations each fail the new tests, corrupting one generated offset fails them too, and `just io_network_check`, `just contracts_check`, `just lint_all`, and `just fmt_check_all` passed.

## Verification

Gates: `just contracts_check`, `just fmt_check_all`, `just io_network_check`, `just lint_all`

## Evidence

[`devlog/2026-08-29-b88-network-destination-generated-offsets/`](../devlog/2026-08-29-b88-network-destination-generated-offsets/index.md).

## Notes

Migrated from `roadmap/00-backlog.md` line 107: `B88 — the network-destination decoder read a Zutai-declared layout through hand-written byte literals`.
