---
schema: work-item/v2
id: 01a0ddaa-8676-7d7e-8faf-47285166731f
kind: epic
state: open
created: 2026-09-26T20:22:15+08:00
updated: 2026-09-26T20:23:32+08:00
tags:
  - io
  - architecture
depends:
  - 01a0ddaa-825f-7309-8508-ed49ffe33a8d
devloop:
  schema: dev-spec/v1
  recordSchema: devloop-consumer/v1
  bodyProfile: fenced-zti/v1
  helper: devloop-helpers/v1:93745c5aa610e8859ed5f5d937e47d55d36ac612cc60b3a22141fa6763fe1c23
  requirementsDigest: 7ce0873df63b7cef065d80c6cb77e4e14164c603b14c16641cea63608d973ec9
  admission: slime-smoltcp-authority-coverage-20260926
  epoch: unstarted
  evidence: []
---

# Broad smoltcp coverage within explicit network authority

```zti
{
  schema = "dev-spec/v1";
  problem = "Slime OS currently enables a small IPv4/Ethernet smoltcp configuration and lacks usable TCP/UDP payload services. After the short-term ROS 2 Zenoh network facilities, the long-term goal is the widest supported smoltcp surface consistent with explicit capability authority, not an ambient BSD-socket personality or an unbounded promise to follow every future upstream feature.";
  scope = [
    "Inventory the pinned smoltcp version and all advertised protocol, socket, medium, configuration and resource options, then implement and qualify every applicable authority-compatible facility in independently trackable slices. docs/plans/network-data-plane.md owns the matrix and rationale; work-item state remains in MyQue.";
    "Cover TCP completion, UDP unicast for DDS transports, bounded DNS, IPv6/NDP and control traffic, multicast/IGMP, address configuration such as DHCPv4 and any supported SLAAC, fragmentation/reassembly, ICMP and alternate media/backends where the pinned library supports them.";
    "Preserve userspace policy, generated versioned Zutai boundaries, exact holders/endpoints/directions and bounded state. Privileged stack control traffic must have explicit service authority and must not become a client bypass.";
  ];
  nonGoals = [
    "Enabling every Cargo feature simultaneously, implementing protocols absent from the pinned smoltcp release, or a moving completion target tied to future releases.";
    "Ambient bind/connect/listen, wildcard or port-range client grants, raw packet injection/sniffing for ordinary applications, resolver-wide access, or trust/discovery responses that create authority.";
    "DDSI-RTPS discovery/reliability/CDR implementation, full Zenoh or ROS compatibility, transport security implementations, and physical NIC qualification; each is separately owned.";
  ];
  requirements = [
    {
      id = "inventory";
      statement = "Maintain a version-bound exhaustive support matrix with an upstream feature/source reference, applicable target/backend, dependency slice, authority mapping, quantitative bounds and verification for each facility. Classify each as supported, planned, excluded for a documented authority-model conflict, or not applicable/not provided by that pinned version. Resource/profile options are not individually new network services. Unsupported because unfinished must remain planned, not excluded.";
      acceptance = [
        "inventory-observed";
      ];
    };
    {
      id = "udp";
      statement = "Provide bounded UDP local bind and per-peer datagrams with message boundaries, source identity, size/truncation semantics, receive queues and independent send/receive rights. Support static DDS participant endpoints without making remote locators or discovered participant ports into grants. Leave RTPS reliability and discovery policy to middleware.";
      acceptance = [
        "udp-observed";
      ];
    };
    {
      id = "network-families";
      statement = "Implement the authority-compatible pinned TCP options, DNS, IPv6/NDP, ICMP/control traffic, routes, supported address configuration and fragmentation/reassembly facilities. Treat a DNS answer, DHCP lease, route advertisement or discovered address as untrusted configuration data, never newly granted client authority. Tie names to bounded resolution/use policy and prevent alternate-address or rebinding bypass. Refuse features whose service/control-plane authority cannot be bounded and represented.";
      acceptance = [
        "network-families-observed";
      ];
    };
    {
      id = "multicast";
      statement = "Admit multicast only with explicit group, interface, port, direction, membership lifetime and bounded sender policy; implement supported group maintenance such as IPv4 IGMP behind that authority. Discovery/scouting remains opt-in userspace policy, and received advertisements cannot authorize unlisted locators. If a required behavior cannot satisfy this boundary, exclude that behavior rather than broaden ambient access.";
      acceptance = [
        "multicast-observed";
      ];
    };
    {
      id = "media-isolation";
      statement = "Assess every pinned medium and backend separately. Keep Ethernet/raw-IP/other framing inside explicitly authorized services, never as raw packet authority for ordinary applications. Implement applicable compatible adapters without assuming virtio-net qualifies another backend or board. Bound neighbor/route/reassembly/socket caches, fairness, timers and state under malformed or saturated traffic.";
      acceptance = [
        "media-isolation-observed";
      ];
    };
    {
      id = "completion";
      statement = "Admit implementation children through the canonical workflow before their code, ordered after short-term Zenoh networking. Publish narrow qualification recipes using the existing network/I/O verification mechanism unless a genuinely distinct boundary requires another owner. Completion requires all admitted facilities and children observed, with maintained exclusions; inventory or TCP/UDP echo alone does not complete broad coverage.";
      acceptance = [
        "completion-observed";
      ];
    };
  ];
  predicates = [
    {
      id = "qualified";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = false;
      upperInclusive = false;
      members = [];
    };
  ];
  acceptance = [
    {
      id = "inventory-observed";
      rationale = "The qualification checks matrix coverage against the pinned dependency feature/protocol surface and checks references. At epic completion every applicable entry must have observed support or a reviewed concrete authority-model exclusion; unresolved planned entries prevent completion. Upgrades trigger a new delta review rather than silently widening claims.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
    {
      id = "udp-observed";
      rationale = "An external peer and a component exchange exact unicast datagrams in both directions; wrong sources/ports/transports are refused and malformed, oversized, reordered, duplicated, lost and full-queue cases remain bounded. This establishes UDP facilities, not DDS interoperability.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
    {
      id = "network-families-observed";
      rationale = "Per-facility positive and adversarial tests cover real bytes, configured bounds and unauthorized control/data refusal, including malformed fragments, resolver replies and configuration expiry/restart where applicable. Each excluded feature records the violated invariant, considered bounded alternative and executable denial or build-time exclusion.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
    {
      id = "multicast-observed";
      rationale = "Qualify admitted group send/receive/join/leave and any control packets through a multicast-capable peer setup, not an unsuitable backend; deny ungranted groups, interfaces, ports, senders and discovered destinations, and reclaim memberships on restart. Distinguish explicit group membership from general LAN discovery support.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
    {
      id = "media-isolation-observed";
      rationale = "Each admitted backend/profile has its own byte-path and isolation evidence, lifetime/restart/resource tests and negative controls. Unsupported or inapplicable media are explicitly classified, and QEMU results carry no physical NIC or IOMMU claim.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
    {
      id = "completion-observed";
      rationale = "The selected version-bound aggregate qualification covers every admitted matrix entry and its refusal/resource tests, verifies the short-term demo network regression, and preserves generated contracts, closure identities, formatting and lint checks. Requirements/evidence identify the tested target and revision.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "qualified";
    };
  ];
}
```
