---
schema: work-item/v2
id: 01a0bd36-8421-7b57-9ac1-c083a2b66993
kind: task
state: open
created: 2026-09-20T13:07:41+08:00
updated: 2026-09-20T13:08:08+08:00
tags:
  - memory-capacity
  - core-runtime
parent: 01a09b65-4386-7b79-a64a-f263de037b8d
depends:
  - 01a0bd36-7f01-76cd-873d-b9c03687fd9f
devloop:
  schema: dev-spec/v1
  recordSchema: devloop-consumer/v1
  bodyProfile: fenced-zti/v1
  helper: devloop-helpers/v1:97a562d90616432e62fb251257465c17632d4955dc04dcb3aabb056344a4be35
  requirementsDigest: 2bbe8be6be1bf1e8efdbd2230b1dfdfa51312805021d97d17c5fbb8c88711f3e
  admission: mem-adaptive-2026-09-20-qualification
  epoch: unstarted
  evidence: []
---

# Qualify adaptive private memory across inventories and regressions

```zti
{
  schema = "dev-spec/v1";
  problem = "No adaptive runtime exit condition is yet observed. Fixed 1 GiB evidence and launcher RAM changes cannot prove same-policy device-derived capacity, exact exhaustion, shared elastic contention, failure recovery or repeated cross-holder reuse.";
  scope = [
    "Before implementation, land this work-item-only planning change, satisfy backlog-first and establish a green baseline. Reuse owning checker mechanisms. Bind all relevant runtime, contracts, generated outputs, build/platform inputs and checker code in the evidence closure; the current work-item-tooling-only devloop codePaths are insufficient. Recipe selection is execution-input data, not a requirement command. Record actual target, revision and image identities; source inspection or pre-existing logs are not fresh runtime qualification.";
    "Own the existing private-memory/platform checkers, sel4_plane execution helpers, build-sel4 platform inventory/package identity handling, deterministic DTB normalization, qualification compositions/probes and bound evidence records. Consume the fully integrated adaptive path.";
    "Qualify at least three kernel-visible ordinary inventories per ARM and RV64 architecture. Candidate launch sizes are ARM 1/2/4 GiB and RV64 1/3/4 GiB; these are proposals, not observed usable inventories. A constrained row and a row with more than 1 GiB resident private payload are mandatory.";
  ];
  nonGoals = [
    "No swap, committed/guaranteed overcommit, RAM hotplug, live heap shrink, relocation, page promotion, OOM killing, new cores/threads, larger shared buffers or ELF limits, or physical-board qualification.";
    "No completion based on implementation status alone; do not fabricate runtime observations, use unrelated passing gates as evidence, or silently reduce the parent acceptance.";
    "No launcher-only memory increase as capacity evidence, admission-only/sequential totals as residency evidence, large-frame-only completion, new top-level checker for each RAM size, or claim that one self-discovering final boot image supports all memory topologies.";
  ];
  requirements = [
    {
      id = "identity-matrix";
      statement = "Build/package named memory inventories with matching kernel-visible DTB/platform ranges and exact platform identities. Within each ABI-compatible architecture use byte-identical root/component binaries and unchanged adaptive policy across all rows. Hash each varying kernel/DTB/loader/package independently; qualify deterministic DTB normalization, including the observed RV64 byte-repeatability discrepancy, without assuming its cause.";
      acceptance = [
        "matrix-residency";
      ];
    };
    {
      id = "resident-scaling";
      statement = "Observe increasing resident working capacity with admitted resources under unchanged private policy and no target page-limit edits. Touch and recheck every committed page, include simultaneous holders, and prove the expanded row has more than 1 GiB usable resident private payload.";
      acceptance = [
        "matrix-residency";
      ];
    };
    {
      id = "exact-exhaustion";
      statement = "For an explicitly pool-authorized bulk holder consume the calculated elastic pool to exact next-request refusal, reconcile all residual ordinary/reusable RAM to reserves, metadata, fragmentation and granularity, and require a still-fitting request to succeed. Repeat with simultaneous holders and mixed/small-page growth; report capacity differences and actual CSlot/descriptor/extent limits.";
      acceptance = [
        "resource-exhaustion";
      ];
    };
    {
      id = "contention-guarantees";
      statement = "Show two maxima can each permit the common pool without the idle first holder preventing peer growth. Never steal committed pages; later pressure fails explicitly with intact data. Guaranteed holders, restart obligations and operational reserves remain serviceable, and denied subjects gain no authority.";
      acceptance = [
        "contention-and-refusals";
      ];
    };
    {
      id = "negative-boundaries";
      statement = "Exercise malformed/unsupported policy, over-guaranteed declarations, duplicate/shared/subtree entitlement evasion, arithmetic overflow, window collisions, device-untyped inclusion and launcher-only inventory mismatch at their owning boundaries. Replay a coordinated identical request schedule to reproduce decisions; classify host versus runtime negative evidence precisely.";
      acceptance = [
        "contention-and-refusals";
      ];
    };
    {
      id = "failure-recovery";
      statement = "Force partial allocation/retype/map/construction failures and near-limit slots/descriptors/extents/metadata including expandable CSpace. Existing mappings and guarantees survive, successful replies always have real backing, retries do not double-charge, and revoke-failure quarantine is recoverable.";
      acceptance = [
        "failure-and-lifecycle";
      ];
    };
    {
      id = "twenty-cycles";
      statement = "With peers retaining full patterns, fault/restart a holder for twenty coordinated cycles. Returned capacity must be used by a different holder, new bytes must be zero, and backing/slots/descriptors/mappings/quota plus charged metadata/anchors reconcile to the same reusable pre-workload baseline without downward drift. Include revoke-failure retry and unchanged shared-buffer operations.";
      acceptance = [
        "failure-and-lifecycle";
      ];
    };
    {
      id = "regression-evidence";
      statement = "Preserve or rerun completed fixed-capacity, isolation, stress, cycles and smaller-budget regressions on the changed mechanisms. Small-profile bootstrap fits or fails before publication. Record exact commands/results, source and image identities, transcript hashes, provenance and retention limits; no physical capacity or immutable archival claim without matching observations.";
      acceptance = [
        "fixed-regressions";
      ];
    };
  ];
  acceptance = [
    {
      id = "matrix-residency";
      rationale = "Six or more qualified executions cover constrained/current/expanded ordinary inventories on both architectures, with verified root/component/policy identity equality within each compatible architecture and separately bound platform/package identities. Full-page residency grows with resources and exceeds 1 GiB in an expanded case; repeated normalized DTB bytes match.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "resource-exhaustion";
      rationale = "Bound transcripts reconcile ordinary-range/backing ledgers, guarantees/reserves, active/reusable/quarantined metadata, payload and limiting resource at exact exhaustion for bulk, all-small and mixed simultaneous-holder schedules. Mutation controls reject invented fit, missing costs and still-fitting refusal.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "contention-and-refusals";
      rationale = "Observed contention preserves guarantees and existing bytes while idle maxima consume no maximum-sized backing. Negative controls cover every declared policy/authority/arithmetic/mapping/platform boundary with the correct host or runtime evidence class; coordinated schedule replay reproduces decisions.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "failure-and-lifecycle";
      rationale = "Both architectures pass injected partial failures, actual growth across management-resource boundaries, revoke-failure retry and twenty coordinated fault/restart cycles with cross-holder zeroed reuse and intact peer patterns. Equivalent resource censuses have no downward drift and shared operations retain their declared budgets.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "fixed-regressions";
      rationale = "Completed fixed-capacity and associated isolation/stress/reclamation plus small-profile regressions remain valid on their exact tested images. Owning generated-contract, build, host, runtime, gate-mutation, formatting, lint and documentation validation passes; the final evidence records every parent exit condition without substituting partial results.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
  ];
  predicates = [
    {
      id = "passed";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = true;
      upperInclusive = true;
      members = [];
    };
  ];
}
```
