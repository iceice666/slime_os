---
schema: work-item/v2
id: 01a0db5f-6918-7b2c-bfa1-57029fc81bac
kind: task
state: open
created: 2026-09-26T09:40:58+08:00
updated: 2026-09-26T09:41:09+08:00
tags:
  - contracts
  - zutai
devloop:
  schema: dev-spec/v1
  recordSchema: devloop-consumer/v1
  bodyProfile: fenced-zti/v1
  helper: devloop-helpers/v1:97a562d90616432e62fb251257465c17632d4955dc04dcb3aabb056344a4be35
  requirementsDigest: 63eeec2776a94ee157dabb6a5eedc52f9d5b484530d3da1261aa85dee21a19f9
  admission: computed-system-specs-2026-09-26-v1
  epoch: unstarted
  evidence: []
---

# Author system specs as computed Zutai sources with derived .zti outputs

```zti
{
  schema = "dev-spec/v1";
  problem = "System specs are the largest hand-authored contract corpus: 49 inert .zti files, about 23.5k lines, with 695 grants, 189 of them init exec/spawn boilerplate, 672 slot pins and 67 minted supervision bindings. Much of it restates facts the corpus already implies. Four RV64 compositions differ from their ARM siblings only in name, generation number and targetRequirement. Every endpoint repeats one grant plus two pins, and every fabric participant copies full QoS fields. Zutai general mode can compute this data: a prototype that imports sel4-private-memory-matrix.zti and overrides three fields with record update evaluates to JSON identical to the committed sel4-private-memory-matrix-rv64.zti. The repository uses Zutai only as a decoder over hand-written data, so it gains neither the computation nor the typing.";
  scope = [
    "Land this work-item-only planning change first, satisfy backlog-first, and establish a green baseline before implementation.";
    "Admit computed Zutai (.zt) authoring sources for system specs. The committed contracts/system-spec/v1/systems/*.zti become derived outputs rendered from those sources, and the existing format-1 schema, decoder, Python compiler, derived generation manifests and image closures continue to consume them unchanged. An inert, hand-authored .zti system spec remains admissible; migration is per composition.";
    "Put the source-to-output mapping, rendering and drift refusal in the existing system-spec generation and check path (scripts/lib/system_spec.py, generate-generation-from-spec.py and just system_spec_check), not in a new top-level checker. Evaluation stays pure: a source may import only other contract sources, and a result that is not first-order data is refused.";
    "Provide a small shared Zutai helper module covering the demonstrated repetition: architecture variants of one composition, executable spawn grants, endpoint channels with their two slot pins, and minted supervision bindings. Add only helpers that a migrated composition actually uses.";
    "Migrate the four RV64 sibling compositions and at least one fabric composition that has channels, such as sel4-call. Update the system-spec contract documentation and the AGENTS.md generated-code rule so the derived .zti are named as outputs.";
  ];
  nonGoals = [
    "No change to the format-1 system-spec record shape, the generation manifest, the boot wire formats or runtime behavior. The derived .zti bytes of every migrated composition must equal the pre-migration committed bytes.";
    "No typed replacement of closed Text vocabularies or QoS union encoding (durability with retained depth, liveliness with lease). That is a format change and belongs to a follow-up item.";
    "No component-signature (port) model, no derivation of component-spec records, and no generation of plane-checker marker expectations from specs. These are separate follow-ups.";
    "No SHA-256, byte encoding or repository-state checks moved into Zutai. They stay in Python.";
    "No Zutai language or stdlib change. Friction found in deps/zutai is reported upstream, not patched here.";
  ];
  requirements = [
    {
      id = "computed-source";
      statement = "A composition may be authored as a pure .zt source that evaluates to a format-1 SystemSpec record. The system-spec compiler resolves each composition to exactly one source (a .zt source or a hand-authored .zti) and refuses ambiguity, a missing source, an effectful or non-first-order result, or an output that fails the existing decoder and semantic validation.";
      acceptance = [
        "derived-identical";
        "drift-refused";
      ];
    };
    {
      id = "derived-output";
      statement = "Each .zt-authored composition has a committed .zti rendering with deterministic formatting. Regeneration is idempotent, and just system_spec_check refuses a derived .zti that differs from its source's rendering, as it already does for derived generation manifests.";
      acceptance = [
        "derived-identical";
        "drift-refused";
      ];
    };
    {
      id = "byte-preserving-migration";
      statement = "Migrating the four RV64 siblings and at least one channel-bearing fabric composition leaves every derived .zti, every derived generation manifest and every system-image closure byte-identical to origin/main at the implementation's base. The migrated sources express architecture variants and endpoint channels through the shared helpers, not by restating whole records.";
      acceptance = [
        "derived-identical";
        "downstream-unchanged";
      ];
    };
    {
      id = "helper-module";
      statement = "The shared helper module is a versioned contract source under contracts/system-spec/v1. Its helpers return format-1 record fragments, have typed signatures, and are used by at least one migrated composition each.";
      acceptance = [
        "derived-identical";
      ];
    };
    {
      id = "ownership-docs";
      statement = "The system-spec contract documentation and AGENTS.md identify which .zti are derived, where their sources live, and which command regenerates them. Maintained documentation checks pass.";
      acceptance = [
        "quality";
      ];
    };
  ];
  acceptance = [
    {
      id = "derived-identical";
      rationale = "The system-spec gate regenerates every .zt-authored composition, and the result equals the committed derived .zti. Every remaining hand-authored composition still validates unchanged.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "drift-refused";
      rationale = "Negative controls in the owning checker: an edited derived .zti, a source with a type error, a composition with both a .zt and a hand-authored .zti, and a source evaluating to non-first-order data are each refused with a diagnostic naming the composition.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "downstream-unchanged";
      rationale = "Composition closure, derived generation manifests and system-image closures resolve without regeneration drift after migration. Nothing a boot consumes changed.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "quality";
      rationale = "Python lint, spelling and maintained documentation checks pass for the changed scripts and docs.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
  ];
  predicates = [
    {
      id = "passed";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = true;
      upperInclusive = true;
      members = [];
    };
  ];
}
```
