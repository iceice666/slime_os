---
schema: work-item/v2
id: 01a0afa7-33e8-7072-8347-635d6159dc45
kind: task
state: cancelled
created: 2026-09-17T21:56:05+08:00
updated: 2026-09-17T22:37:35+08:00
closed: 2026-09-17T22:37:35+08:00
devloop:
  schema: dev-spec/v1
  recordSchema: devloop-consumer/v1
  bodyProfile: fenced-zti/v1
  helper: devloop-helpers/v1:97a562d90616432e62fb251257465c17632d4955dc04dcb3aabb056344a4be35
  requirementsDigest: a45dc6401970cfd8de59d469fca92edc4f88be3fe66750fa169fd2e2537e1a0d
  admission: slime-devloop-mandate-proof-2026-09-17
  epoch: unstarted
  evidence: []
---

# Exercise the spec-driven mandate end to end through devloop

```zti
{
  schema = "dev-spec/v1";
  problem = "The spec-driven mandate and the generic just-target gate landed in #100, but no work item has ever been carried through devloop in this repository: the store holds zero items with recorded evidence, and the just-target gate has never written an evidence entry. The mechanism that now refuses every new prose item is itself unexercised, and its own planning item cannot honestly close until a real gate invocation has recorded a real observation.";
  scope = [
    "Carry one item through the full devloop lifecycle: admit, start, gate, complete.";
    "Record evidence from the generic just-target gate for two different just recipes, proving a recipe name supplied in execution inputs reaches the gate and produces a passed observation.";
    "Leave the recorded evidence in the store as the first worked example contributors can read.";
  ];
  nonGoals = [
    "Changing the cutoff instant, the mandate, or any gate policy that #100 established.";
    "Declaring per-target typed observations, which the generic gate exists to avoid.";
    "Retrofitting or migrating any pre-cutoff prose item.";
  ];
  requirements = [
    {
      id = "R1";
      statement = "The just-target gate runs just tasks_check through devloop and records a passed observation bound to that recipe.";
      acceptance = [
        "A1";
      ];
    };
    {
      id = "R2";
      statement = "The just-target gate runs just docs_check through devloop and records a passed observation bound to that recipe, from a second invocation whose execution inputs name a different recipe.";
      acceptance = [
        "A2";
      ];
    };
  ];
  acceptance = [
    {
      id = "A1";
      rationale = "Observe the generic gate execute the work-item store check named in execution inputs";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "P1";
    };
    {
      id = "A2";
      rationale = "Observe a second invocation bind to a different recipe, so evidence is per target and not per gate";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "P2";
    };
  ];
  predicates = [
    {
      id = "P1";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = true;
      upperInclusive = true;
      members = [];
    };
    {
      id = "P2";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = true;
      upperInclusive = true;
      members = [];
    };
  ];
}
```
