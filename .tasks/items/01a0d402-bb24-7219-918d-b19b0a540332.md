---
schema: work-item/v2
id: 01a0d402-bb24-7219-918d-b19b0a540332
kind: bug
state: done
created: 2026-09-24T23:22:23+08:00
updated: 2026-09-25T11:39:12+08:00
closed: 2026-09-25T11:39:12+08:00
tags:
  - backlog
  - memory-capacity
  - core-runtime
related:
  - 01a09b65-4386-7b79-a64a-f263de037b8d
---

# Elastic rollback quarantine is never retried in production, its single slot can be overwritten, and one failure path does not quarantine the ledger member

## Context

Found by the 2026-09-24 post-closure stress audit of MEM-ADAPTIVE (source
reading; needs a kernel revoke failure).

`object_allocator/elastic.rs` stores a failed rollback in one
`elastic_quarantine` slot, which a second failure overwrites;
`retry_elastic_quarantine` is called only from
`private_memory/qualification.rs`. `settle_failure` quarantines the ledger
member forever (its `begin` returns `Cleanup` until retirement), while
`settle_refusal` and the lend-failure path do not quarantine the member at all.
`docs/architecture/private-memory.md` states that the next request is refused
and exactly one retry returns the resources; neither half holds in production.
Arena revocation still reclaims the backing at task death, so the effect is a
growth-dead incarnation and a stale slot rather than a lost range.

## Exit conditions

Every failed rollback is retained per arena and retried by production code, the
ledger member's quarantine matches the allocator's on every path, and the
documented behavior is what the code does.

## Verification

Gates: `just test_sel4_root`, `just private_memory_adaptive_lifecycle_check`

## Closure evidence

Fix commit `ba2bc28`. Failed rollbacks accumulate in a per-arena `Quarantine` record instead of one global slot; a quarantined holder's next growth retries the return and is refused with `Cleanup`, and arena release clears the record. `settle_refusal` and the lend-failure path quarantine the ledger member whenever the allocator kept extents. `docs/architecture/private-memory.md` describes this behavior. Regression test `failed_rollbacks_accumulate_per_arena`; the rollback arm's quarantine stage (owned=1 refused=1 released=1 remaining=0) passed on both architectures. Limit: the production retry-on-next-growth path runs on the rollback arm's qualification holder, not on a spawned component.

Branch `fix/mem-adaptive-stress-audit` (parent `9fee40d`). `just
private_memory_phase5_check` passed at `5583149` with exit 0
(`build/phase5-fix3to10.log`, sha256
`95ed582823f35fad2a92258cde44742b1bd2daab0e675e88f31683cf47f6b7cd`, QEMU
11.0.1): all six matrix rows (arm 1/2/4 GiB, rv64 1/3/4 GiB), the adaptive
workloads on both architectures, adaptive-lifecycle, and the elastic,
fragmentation, rollback, conservation, cspace, metadata, bootstrap, capacity
and stress arms, plus `test_sel4_root` 329/329 and `sel4_gate_control_check`
(45 gates plus 9 adaptive arms rejecting 2763 mutated transcripts).
`just test_host`, `just fmt_check_all`, `just lint_all` and `just docs_check`
passed. These are QEMU observations only; no hardware claim.
