---
schema: work-item/v1
id: 01a00b4d-2400-7c5a-86f1-3469a46c2c25
key: B59
kind: bug
state: done
created: 2026-08-17T00:00:00+08:00
closed: 2026-08-17T00:00:00+08:00
tags:
  - backlog
---

# the syscall ABI had no single source: 97 rights declarations, two label tables, three error tables

## Context

Resolved 2026-08-17. **Class:** Unmasked architectural debt.

four number tables crossing the root/userspace boundary — rights (97 sites), operation labels, error codes, and the spawn-grant record layout — were hand-authored in multiple places with nothing forcing agreement, a class of defect that had already caused a silent keystroke-misdecode regression.

Class: Unmasked architectural debt.

## Exit conditions

a new `contracts/syscall-abi/v1` contract generates the one shared module both crates consume; 97 hand-written rights sites collapsed to 1 sentinel; regression tests pin all 23 labels, 6 status codes, and the record layout, verified to bite by renumbering a label and observing the freeze test abort. `just contracts_check`, `just generation_check`, `just test_host`, `just test_sel4_root` (118/118), `just architecture_contract_check`, `just sel4_root_boot_check`, the full seL4 plane suite, `just fmt_check_all`, `just lint_all`, and `just ruff` pass.

## Verification

Gates: `just contracts_check`, `just fmt_check_all`, `just lint_all`, `just ruff`, `just sel4_root_boot_check`, `just test_host`, `just test_sel4_root`

## Evidence

[`devlog/2026-08-17-b59-b66-syscall-abi-contract/`](../devlog/2026-08-17-b59-b66-syscall-abi-contract/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 478: `B59 — the syscall ABI had no single source: 97 rights declarations, two label tables, three error tables`.
