---
schema: work-item/v2
id: 01a0d6d1-c21e-7d1d-aafb-8ea9fa39e981
kind: bug
state: active
created: 2026-09-25T12:27:46+08:00
updated: 2026-09-25T16:33:40+08:00
tags:
  - backlog
  - memory-capacity
  - core-runtime
related:
  - 01a0d402-ba1c-7ff1-a9b7-63dbada37e12
devloop:
  schema: dev-spec/v1
  recordSchema: devloop-consumer/v1
  bodyProfile: fenced-zti/v1
  helper: devloop-helpers/v1:97a562d90616432e62fb251257465c17632d4955dc04dcb3aabb056344a4be35
  requirementsDigest: ad7eb067d2a08e2257d90e8881fe2bcc103a803edc7d00d0cff848d9052facb8
  admission: metadata-reserve-01a0d402-ba1c-7ff1-a9b7-63dbada37e12-v1
  epoch: 169fa1cf-4e5e-453a-8019-05c060454976
  evidence: []
---

# Account root metadata growth and protect adaptive operational reserves

```zti
{
  schema = "dev-spec/v1";
  problem = "Follow up canonical defect 01a0d402-ba1c-7ff1-a9b7-63dbada37e12: adaptive private-memory demand charges payload backing but not root metadata ownership growth. The ledger can advertise free bytes after infrastructure has consumed the operational reserve, and the existing matrix checks only a residual upper bound and construction after bulk exhaustion.";
  scope = [
    "Root-owned metadata accounting and allocator enforcement of adaptive operational headroom, including source adoption, metadata directories, mapping tables and CSpace growth.";
    "Transactional acquisition, failed-growth retention, holder reclamation and reusable metadata without double charging or invented refunds.";
    "Existing ARM and RV64 inventory-matrix schedules, mixed-pressure construction, accounting diagnostics and checker negative controls.";
    "Current private-memory architecture documentation and generated system specifications and image closures affected by the implementation.";
  ];
  nonGoals = [
    "Physical-machine memory qualification, new hardware support, live shrinking or stealing holder memory.";
    "A fixed percentage inferred from historical metadata measurements, or a larger fixed reserve without allocator enforcement.";
    "Weakening physical placement certification, allocating maximum-device metadata at boot, or silently changing fixed-v1 guarantees.";
  ];
  requirements = [
    {
      id = "root-ownership";
      statement = "Charge newly infrastructure-owned capacity to a root-owned account, not a reclaimable holder account. Charge the complete ordinary tail or returned extent removed from common inventory, including unused adopted backing, exactly once; allocating metadata from already charged backing does not charge it again. Existing boot commitments are not deducted twice. Checked arithmetic refuses overflow.";
      acceptance = [
        "accounting-regression";
        "matrix-pressure";
        "full-regression";
      ];
    };
    {
      id = "reserve-funding";
      statement = "Before elastic acquisition or infrastructure ownership transfer can consume common inventory, secure its complete funding while preserving operational headroom. Post-admission static construction costs must be explicit, not silently attributed to metadata. The configured reserve is the required free floor at every qualified exhaustion checkpoint before the operational construction probe; static admission and construction commitments must be priced so these checkpoints retain the full floor. An explicitly operational construction may spend its protected funding, with its consumption and reclamation reported separately. Admission refuses an unaffordable combination of guarantees, system commitments and reserve.";
      acceptance = [
        "accounting-regression";
        "matrix-pressure";
        "full-regression";
      ];
    };
    {
      id = "transaction-lifetime";
      statement = "Failure before or after ledger judgment cannot leave uncharged infrastructure ownership or consume protected headroom. Abort, quarantine and retry retain charges for resources still owned, settle exactly once, and refund only capacity actually returned to the eligible pool. Holder exit does not refund root-lifetime metadata; reuse of existing metadata is not charged again. Acquisition cannot rely on a post-effect inventory check to restore irreversibly adopted backing.";
      acceptance = [
        "accounting-regression";
        "full-regression";
      ];
    };
    {
      id = "placement-and-guarantees";
      statement = "Preserve exact physical-placement certification for holder backing and separately certify root ownership changes. Guarantee redemption remains funded from protected resources under elastic exhaustion; incarnation isolation, failure rollback and fixed-v1 behavior remain unchanged. Reserve bytes alone are not evidence of placeable construction: slots, descriptors, extents, tables and their supporting metadata must also be available.";
      acceptance = [
        "accounting-regression";
        "matrix-pressure";
        "full-regression";
      ];
    };
    {
      id = "matrix-residual";
      statement = "On every pinned ARM and RV64 inventory row, the final single-page refusal of bulk, all-small and both mixed holders leaves allocator residual at least the declared operational reserve before an operational probe. Retain an upper-bound exhaustion check whose allowance is derived from the funded allocation and metadata model, not unexplained extra headroom. Report ledger free bytes, allocator free bytes, root-owned growth, protected reserve and the actual refusal resource so the measured ownership changes reconcile.";
      acceptance = [
        "matrix-pressure";
        "negative-controls";
      ];
    };
    {
      id = "mixed-construction";
      statement = "After both mixed holders reach exhaustion, construct and run an independently declared probe while both pressure holders remain live and retain their full verified contents. Do not release either pressure holder or the guaranteed holder to fund this construction. Observe successful construction and cleanup and continued refusal of unauthorized private growth; preserve guarantee-pressure, residency-scaling and twenty-cycle reuse checks.";
      acceptance = [
        "matrix-pressure";
        "negative-controls";
      ];
    };
    {
      id = "refusal-controls";
      statement = "The existing verification mechanism rejects residual below reserve, missing or misordered mixed-pressure construction, construction only after releasing a pressure holder, unexplained residual above the derived bound, and missing accounting evidence. Host tests cover metadata and directory boundaries, CSpace expansion, whole-source adoption, failure retention, holder retirement and reuse without double charges.";
      acceptance = [
        "accounting-regression";
        "negative-controls";
      ];
    };
    {
      id = "integration";
      statement = "Document root versus holder cost ownership and the reserve lifecycle in the owning architecture documentation. Keep serialized formats in versioned Zutai sources and regenerate affected bindings, composition manifests and system-image closures. Publish exact target, revision and tested image identities with observed verification and no physical-hardware claim.";
      acceptance = [
        "full-regression";
        "quality";
      ];
    };
  ];
  predicates = [
    {
      id = "passed";
      kind = "equal";
      observation = "passed";
      expected = {
        kind = "bool";
        intValue = 0;
        boolValue = true;
        textValue = "";
      };
      lower = 0;
      upper = 0;
      lowerInclusive = true;
      upperInclusive = true;
      members = [];
    };
  ];
  acceptance = [
    {
      id = "accounting-regression";
      rationale = "Host regression proves root ownership, pre-effect funding, settlement and protected-resource invariants.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "matrix-pressure";
      rationale = "All pinned inventory rows prove both residual bounds and construction with mixed holders still resident.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "full-regression";
      rationale = "The complete adaptive and fixed-memory regression surface remains qualified against the changed mechanism.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "negative-controls";
      rationale = "Mutation controls prove malformed or missing pressure and reserve evidence cannot pass.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
    {
      id = "quality";
      rationale = "Formatting, lint, documentation, contract and generated-artifact checks pass for every affected owner.";
      gate = "just-target";
      mode = "automated";
      mandatory = true;
      predicate = "passed";
    };
  ];
}
```
