---
schema: work-item/v1
id: 01a09b65-4386-7b79-a64a-f263de037b8d
key: MEM-ADAPTIVE
kind: epic
state: open
created: 2026-09-13T23:31:40+08:00
updated: 2026-09-20T13:08:08+08:00
tags:
  - memory-capacity
  - core-runtime
depends:
  - 01a07a2d-003d-77fc-9df8-dda85ed9a083
  - 01a07a2d-0081-7df4-b7fe-38b933057b4a
  - 01a0bd36-35c3-7993-8fb4-230ebfdd70bf
  - 01a0bd36-74c0-7f35-9b74-d4fdc99a1f4b
  - 01a0bd36-79dc-7e4a-a913-ecc793b70062
  - 01a0bd36-7f01-76cd-873d-b9c03687fd9f
  - 01a0bd36-8421-7b57-9ac1-c083a2b66993
related:
  - 01a07a2c-9c4f-7ca0-8417-aba2b48ce16b
---

# Device-derived private-memory capacity and demand-backed use of spare RAM

## Context

Use the ordinary RAM a device actually hands to root automatically, rather than
replace one fixed private-memory ceiling with a larger constant. Under unchanged
explicit workload policy, a larger kernel-visible inventory must permit a larger
private working set. An idle holder's elastic maximum must not reserve the pool.
Available means seL4 BootInfo ordinary RAM after exclusions, less measured system
commitments and declared reserves; it never means installed RAM or MMIO.

This is an independent continuation of the fixed-capacity work, not another child
of its original five-child scope. The segmented-backing and ordinary-inventory
prerequisites remain dependencies. The fixed qualification
`01a07a2d-00a1-727c-a548-130a4475f5bb` is now done, with a 2026-09-19 closure
record for four simultaneous 256 MiB holders on ARM and RV64 QEMU. Preserve or
rerun that evidence when shared mechanisms change. Its parent epic's open state
is not the child's state and does not make fixed capacity adaptive.

Current architecture: [private component memory](../../docs/architecture/private-memory.md).
The [original research](https://git.justaslime.dev/iceice666/slime_os-history/src/commit/45ed1745907b2d0a13fdf70c8b34eb635bed5f23/devlog/2026-09-13-mem-adaptive-device-capacity/index.md)
is historical rationale, not a current capacity or completion claim.

## Observed starting point

Research refreshed on 2026-09-20 against the current source and the fixed
qualification's recorded evidence. No adaptive runtime behavior was implemented
or qualified by that research.

- `contracts/private-memory-budget/v1/schema.zt` publishes 65536 pages
  (256 MiB) per holder and 262144 pages (1 GiB) aggregate for ARM/RV64 QEMU.
  Other targets retain 512/2048-page defaults. These are qualified fixed-policy
  bounds, not measurements of device RAM.
- `TaskTable::create` and `ObjectAllocator::provision_private_backing` reserve
  the entire quota's backing, CSlots and descriptors before publication. Mapping
  is lazy; reservation is not. Executing the current pure `plan_task_backing`
  function in a temporary host harness showed one page reserves 2101248 bytes
  (2 MiB data plus 4 KiB table backing); 65536 pages reserve 268959744 bytes and
  require 66049 planner CSlots before the remaining static task costs. The planner
  refuses a single holder above 256 MiB.
- Actual quota lookup is by declared instance identity, not executable or TaskId.
  A declared instance cannot have two live incarnations, but separately declared
  instances each carry independent fixed promises. Adaptive shared entitlement
  membership must be explicit; spawn count is not a memory-budget ledger.
- The private total counts committed payload, not all owned backing. In-task
  `free` recycles inside the live component. Failed growth preserves existing
  bytes; task-level charges are released only after successful revocation.
- Root has a 524288-slot bitmap, a 19-bit packed slot field, fixed allocation and
  extent arrays, and compile-time per-region leaf bitmaps. Ordinary alignment
  prefixes are now preserved, but the preserved-leaf registry is still bounded
  to 4096/256 entries and retains ownership. None is an adaptive endpoint.
- The two existing capacity logs match the SHA-256 values in the fixed
  qualification's closure record. Their allocator summaries report 1582915120
  remaining ordinary bytes / 522071 free slots on ARM and 3193724352 bytes /
  522096 slots on RV64. The RV64 byte count alone corresponds to 779717 whole
  4 KiB frames, exceeding those free slots by 257621 before metadata or table
  costs. These are inherited, image-specific observations, not a fresh boot or
  a capacity-fit calculation; do not double-subtract boot exclusions.
- `child_vspace::private_window` aligns the base to the whole compiled window
  size before checking 2 MiB alignment. A temporary harness executing that exact
  function with 257 MiB and 1537 MiB sizes refused both. Separate base alignment
  from reservation length, and recompute table costs and mapping exclusions.
- Root's 512 KiB heap cannot simply hold replacement device-sized vectors.
  Metadata needs explicitly backed and mapped storage plus a bounded bootstrap
  path whose own slots, tables and descriptors are protected and charged.
- `scripts/build/build-sel4.py::dump_device_tree` takes memory from platform
  pins. A launcher-only increase cannot enlarge the kernel's admitted inventory.
  DTB-only probes covered ARM 1/2/4 GiB and RV64 1/3/4 GiB; the memory nodes
  matched, but no seL4 boot or payload residency was measured. ARM repeated DTB
  bytes matched. RV64 bytes still differed after the current rng-seed deletion
  although rendered DTS matched and re-emitted DTBs matched. Investigate and
  qualify normalization in the platform matrix; no root cause is asserted here.

## Scope

### Device-derived capacity and accountable resource pools

- Derive effective capacity from actual BootInfo ordinary ranges and reusable
  backing, with checked arithmetic and exact extent/alignment fit. Account for
  boot exclusions only once: memory already absent from BootInfo is not deducted
  again. Separately charge root metadata, kernel objects allocated by root,
  staged/static tasks, page tables, shared-buffer/DMA commitments, and an explicit
  operational/restart reserve. Do not consume device-untyped regions.
- Size or grow bounded CSpace/descriptor/span tracking from admitted resources,
  with a small bootstrap path. Resolve packed-field, extent-index and root-image
  feedback constraints. Do not compile maximum-device-sized arrays into every
  board image or merely widen the old target page constants. Bootstrapping the
  metadata must itself fit and be included in the resource ledger.
- Report ordinary inventory, excluded/committed system costs, guaranteed backing,
  elastic reserved extents, committed payload, metadata/slots, reusable capacity,
  policy limits and the actual limiting resource separately. Residual unusable
  RAM must have an observed reason, not be hidden behind a fixed GiB ceiling.

### Explicit adaptive authorization, not implicit authority

- Define adaptive private-memory policy in a versioned Zutai contract and generate
  every persisted/IPC binding. Separate holder opt-in, guaranteed capacity,
  maximum authorized growth and the device-derived shared elastic pool. Permit
  an explicit pool-relative authorization so a normal capacity increase requires
  no pageQuota or per-target private-capacity table edit.
- The admitted generation carries deterministic policy, not a host machine's
  resolved free-RAM number. Resolve device-dependent capacity from the trusted
  boot inventory; identical policy and inventory produce identical admission
  results. Policy is declared by the composition/userspace owner; root implements
  accounting, validation and allocation, not application importance heuristics.
- An omitted holder has no private-memory authority even when RAM is idle.
  Guarantee, authorization maximum and current availability are distinct. All
  guaranteed commitments must be simultaneously affordable; elastic maxima are
  permission to request, never promises that all maxima can be satisfied together.
  Successful growth must always obtain real backing before success is returned.
- Specify contention ordering and instance/subtree accounting. Repeated spawn of
  one component cannot multiply its declared guaranteed entitlement or evade a
  shared policy budget. Preserve explicit fixed-budget semantics through an
  intentional version/migration decision; never reinterpret an old quota as
  best-effort elastic capacity silently. Migrate affected callers/fixtures/docs
  in one coherent cutover, with no competing hand-written format or alias path.

### Demand-backed elastic growth and lifecycle

- Reserve a stable guarded virtual window at task construction, independently of
  physical commitment. Native pointers and the base remain stable for the task
  lifetime. Check address ceiling, large-frame alignment, upper/leaf table costs
  and collisions with every other mapping path for device-derived window sizes.
- Reserve guaranteed resources before publication. Obtain elastic backing,
  descriptors and slots from a common reclaimable pool as growth requires them,
  rather than sequestering every holder's authorized maximum at spawn. Preflight
  and commit the complete resource tuple transactionally. Refusal leaves prior
  mappings, contents and guarantees usable.
- Preserve exact page accounting with mixed 4 KiB/2 MiB mappings. Small-page and
  fragmented paths must use available resources safely, not rely on successful
  huge mappings or silently promise unrepresentable fallback capacity.
- Roll back newly acquired elastic resources on failed growth/construction where
  safe; any required retained table/extent remains explicitly charged and reusable,
  not invisible leakage. Release backing/charges only after successful revocation;
  failed teardown keeps ownership until retry. Task death returns capacity for
  other holders. No automatic stealing or shrinking of live private bytes.

### Platform and validation integration

- Reconcile host component-spec/system-spec/generation validation with runtime
  resolution, including malformed/unsupported policy versions and overflow cases.
  Keep policy identity distinct from the identity-bound boot platform inventory.
- Qualify multiple RAM configurations per QEMU architecture. Use unchanged root
  and component binaries plus unchanged adaptive policy when the kernel ABI is
  unchanged; platform kernel/loader/DTB artifacts may differ where upstream seL4
  requires it, and every run must name the actual identity. Do not claim a single
  self-discovering boot image if RAM topology is still compiled into the kernel.
- Record physical-board admission/boot constraints separately. The mechanism must
  not hardcode QEMU target-name capacity rows as its source of available RAM, but
  QEMU evidence alone cannot qualify a board or all installed hardware memory.

## Implementation decisions and sequence

The following is the proposed implementation direction, not existing behavior:

1. Specify versioned policy and entitlement accounting. Separate guaranteed pages,
   explicit pool-relative/fixed maximum permission and actual commitment; keep
   fixed budgets fully guaranteed. Bind instances to stable named entitlements,
   including explicitly declared shared/subtree membership, so incarnations do
   not multiply guarantees. Service elastic requests in root receipt order with
   guarantees and reserves protected; no fairness or stealing heuristic is implied.
2. Establish expandable CSpace and segmented metadata with a small non-recursive
   bootstrap. Migrate all capability-address users coherently. A fixed 19-bit
   experiment is an intermediate probe, never this epic's endpoint. Charge and
   reclaim or explicitly reuse metadata, retained anchors and backing; no
   maximum-device arrays or hidden lifetime growth.
3. Acquire elastic backing, descriptors, slots and tables transactionally on
   demand. Prefer large mappings when they fit, but support page-granular backing
   from fragmented ordinary/reusable RAM. Quarantine failed cleanup; release
   ownership only after successful revocation. Preserve existing mappings and
   guarantee resources on refusal.
4. Resolve stable guarded task windows and integrate admission, spawn, growth,
   restart and teardown. Align the base independently of the page-aligned window
   length, account upper tables and protect the entire unbacked window against
   all other mapping paths. Fixed and adaptive modes use one coherent mechanism.
5. Qualify three inventories per architecture with identical adaptive policy and
   root/component binary identities within each ABI-compatible architecture.
   Candidate launch sizes are ARM 1/2/4 GiB and RV64 1/3/4 GiB, not qualified
   ordinary inventories. Bind each kernel/DTB/package to its own identity, resolve
   DTB repeatability, exercise the complete failure/contention/reclamation matrix
   and preserve the completed fixed-capacity regressions.

### Canonical implementation tasks

1. [Define adaptive private-memory policy and entitlement accounting](01a0bd36-35c3-7993-8fb4-230ebfdd70bf.md) — `01a0bd36-35c3-7993-8fb4-230ebfdd70bf`.
2. [Implement expandable root CSpace and reclaimable metadata bootstrap](01a0bd36-74c0-7f35-9b74-d4fdc99a1f4b.md) — `01a0bd36-74c0-7f35-9b74-d4fdc99a1f4b`.
3. [Implement demand-backed private allocation and transactional rollback](01a0bd36-79dc-7e4a-a913-ecc793b70062.md) — `01a0bd36-79dc-7e4a-a913-ecc793b70062`.
4. [Integrate adaptive windows admission spawn and memory lifecycle](01a0bd36-7f01-76cd-873d-b9c03687fd9f.md) — `01a0bd36-7f01-76cd-873d-b9c03687fd9f`.
5. [Qualify adaptive private memory across inventories and regressions](01a0bd36-8421-7b57-9ac1-c083a2b66993.md) — `01a0bd36-8421-7b57-9ac1-c083a2b66993`.

Canonical child identities and dependency edges are owned by MyQue metadata.
These stages are independently verifiable contracts/mechanisms; host-only stages
must reject unsupported adaptive activation rather than publish a no-op runtime.
The final qualification stage cannot substitute a large-frame-only benchmark for
small-page behavior, or reduce any exit condition below.

## Exit conditions

- On both QEMU architectures, run the same adaptive policy and root/component
  binaries against at least three qualified ordinary-memory inventories, including
  a constrained case and a case with more than 1 GiB of usable private payload.
  With no private page-limit edits, observed resident capacity increases with
  resources. Touch and recheck every committed page; sequential allocation totals
  and admission-only reports do not count as residency evidence.
- For an uncapped, explicitly authorized bulk holder, consume the calculated
  elastic pool up to the exact next-request resource refusal. Reconcile remaining
  ordinary/reusable RAM to named reserves, metadata, physical fragmentation and
  granularity; a still-fitting next request must succeed. Repeat with multiple
  simultaneous holders and mixed small/large growth, not just an optimistic
  large-frame benchmark. Report small-page versus bulk capacity differences.
- Two elastic holders can spawn with maxima that each permit the common pool:
  the first remaining idle does not prevent the second from using spare RAM.
  Once backing is committed it cannot be stolen. A later growth request under
  pressure fails explicitly without corrupting either holder. Guaranteed holders
  and the declared operational reserve remain serviceable under that pressure.
- Denied holders, duplicate-instance entitlement evasion, over-guaranteed policy,
  malformed versions, arithmetic overflow, reserved-window collisions, MMIO
  inclusion and launcher-only memory changes fail the owning boundary. Identical
  policy/inventory and the same request schedule reproduce the same decisions.
- Force partial allocation/map/construction failures and near-limit
  CSlot/descriptor/extent conditions. Existing bytes and guarantees survive;
  retries do not double-charge. Refused allocation cannot report success without
  backing. No swap, demand-zero overcommit or best-case-only guarantees.
- While peers retain patterns, fault and restart a holder for 20 coordinated
  cycles; returned capacity is usable by a different holder, new bytes are zero,
  and teardown returns reusable backing, slots, descriptors and quota totals to
  the pre-workload baseline without downward drift. Include a revoke-failure
  retry and shared-buffer operations within their unchanged declared budget.
- Existing fixed-budget private-memory behavior and the completed fixed-capacity evidence
  remain valid under their own policy. Small-target bootstrap metadata fits the
  admitted profile or fails explicitly before publication; no physical capacity
  claim is made without a board-specific inventory and runtime observation.

## Non-goals

Swap, overcommit of guaranteed/committed backing, RAM hotplug, relocating native
heaps, automatic live shrink/decommit, compaction/page promotion, OOM killing,
new cores/threads, larger shared buffers or ELF limits, 1 GiB frame support, and
unqualified physical-board support are not implied. In-task `free` still does
not mean return-to-system; live decommit is a separate explicit contract decision.
Using spare RAM means serving authorized demand, not eagerly filling all RAM.

## Verification

Extend existing private-memory, allocator, platform and gate-control mechanisms;
do not add one top-level checker per RAM configuration. Relevant existing gates
include `just private_memory_check`, `just private_memory_stress_check`,
`just private_memory_isolation_check`, `just private_memory_cycles_check`,
`just sel4_pin_check`, `just sel4_root_boot_check`,
`just sel4_gate_control_check`, `just contracts_check`,
`just generation_check`, `just component_spec_check`,
`just system_spec_check`, `just system_image_builder_check`,
`just test_sel4_root`, `just test_host`, `just fmt_check_all`,
and `just lint_all`. Regenerate affected system-image closures before runtime
qualification. A green existing fixed-capacity gate alone does not qualify any
new adaptive acceptance.

Land the work-item-only planning change before implementation; satisfy
backlog-first and establish the required green baseline before starting runtime
work. New children use devloop admission and evidence-bound completion. The
current consumer policy's codePaths cover work-item tooling, not the memory
runtime: bind the complete tested runtime/build/contract/checker closure in the
owning verification policy before recording implementation completion evidence.
Gate recipe names belong in execution inputs, not requirement text.

This epic remains open. All adaptive runtime exit conditions remain unobserved.
Close only after every child and the complete cross-stage exit conditions are
observed, recording exact commands, target/revision/image and transcript identities,
evidence provenance and retention limits. Use the current canonical closure-record
policy; an ordinary implementation requires no separate devlog or claim of
immutable remote transcript retention. Research calculations and DTB probes are
not seL4 runtime qualification.
