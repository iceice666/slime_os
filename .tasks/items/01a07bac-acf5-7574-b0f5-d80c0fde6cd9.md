---
schema: work-item/v1
id: 01a07bac-acf5-7574-b0f5-d80c0fde6cd9
key: IO8
kind: milestone
state: done
created: 2026-09-07T11:41:49+00:00
updated: 2026-09-14T15:26:18+00:00
closed: 2026-09-14T15:26:18+00:00
tags:
  - io
---

# Declared-device authority and the pwm-servo protocol

## Context

The IO substrate resolves a driver's MMIO authority only by probing the virtio transport window,
so a physical device at a fixed address cannot be granted to a userspace driver at all. This
adds the missing half -- a device the generation *declares* rather than one the root discovers --
together with the first device-specific protocol that uses it, a servo-PWM contract.

The mechanism and the protocol are board-neutral: the driver refuses to program anything unless
its granted page is the device it expects, so the same composition boots under QEMU with the
driver resident and reporting no device.

## Exit conditions

Under QEMU: the `sel4-pwm` composition admits, its driver binds no device and stays resident
answering the protocol's no-device status, graph health stays green, and the boot layout matches
its frozen fixture. On the host: the protocol's Rust and C encoders agree on one pinned request
vector, and the declared-device inventory is covered by a root unit test.

## Verification

Gates: `just contracts_check`, `just generation_check`, `just sel4_boot_layout_check`,
`just sel4_component_graph_check`, `just slisp_core_check`, `just io_driver_authority_check`,
`just test_host`, `just test_sel4_root`

## Closure

Closed 2026-09-14 on the observed exit. `just sel4_pwm_graph_check` booted the
`sel4-pwm` composition (generation 55) on QEMU: the root admitted seven
executables, init launched `pwm-servo-driver` at slot 10 before Slisp, the root
installed its declared quota with no device behind it, the driver printed
`device absent, refusing requests` and stayed resident, `(pwm 0 1600)` came back
`! pwm no-device`, the supervisor certified `required=5 live=5 idle=5 failed=0`,
and the boot layout matched its blessed fixture (7 slots). On the host,
`components/proto/tests/pwm_servo.rs` and `check-slisp-core.py` pin the same
32-byte request, and `slime-root`'s inventory tests cover `declared`. Entry:
`devlog/2026-09-14-io8-pwm-servo/`. The register model behind the driver is a
platform's, supplied in `components/services/pwm-servo-driver/src/block.rs`;
this tree carries none.
