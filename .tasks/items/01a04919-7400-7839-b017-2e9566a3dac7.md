---
schema: work-item/v1
id: 01a04919-7400-7839-b017-2e9566a3dac7
key: B83
kind: bug
state: done
created: 2026-08-29T00:00:00+08:00
closed: 2026-08-29T00:00:00+08:00
tags:
  - backlog
---

# The root still owns the product virtio-blk path after IO2's parity proof

## Context

Resolved 2026-08-29. **Class:** Change (dead-code cutover of an unreachable product path).

`slime-root/src/virtio_blk.rs`, `console.rs::serve_block_transact`, the `ConsoleKind::BlockTransact` label, and the `block_transact*` runtime wrappers were still compiled into every product image and the post-admission `probe_devices` call could still construct a root-owned block driver, so the root retained virtio opcode and descriptor parsing that no seL4 composition could reach.

Class: Change (dead-code cutover of an unreachable product path).

## Exit conditions

The parser survives only as `boot_selector_block.rs` under `#[cfg(slime_boot_selector)]` for the acknowledged pre-admission bootstrap read; all eight migrated planes plus `just io_block_check`, `sel4_qemu_image_check`, `sel4_boot_check`, `sel4_device_check`, and `sel4_boot_selection_check` passed, and `sel4_component_graph_check` now fails if any retired symbol returns.

## Verification

Gates: `just io_block_check`

## Evidence

[`devlog/2026-08-29-b83-root-block-path-deleted/`](../devlog/2026-08-29-b83-root-block-path-deleted/index.md).

## Notes

Migrated from `roadmap/00-backlog.md` line 131: `B83 — The root still owns the product virtio-blk path after IO2's parity proof`.
