---
schema: work-item/v1
id: 01a08ac5-14ba-7d23-a751-b06678692de9
key: IO9
kind: milestone
state: done
created: 2026-09-10T10:02:46+00:00
updated: 2026-09-15T06:01:50+00:00
closed: 2026-09-15T06:01:50+00:00
tags:
  - io
depends:
  - 01a07bac-acf5-7574-b0f5-d80c0fde6cd9
---

# Bounded serial transmit and a clock-paced heartbeat producer

## Context

The IO substrate has no way for a component to transmit on a serial port: `AuthorityInventory`
is populated only by a virtio-mmio probe, and the root's UART adapter reads a byte for the
console rather than writing one. A component also cannot pace itself in real time, because
`monotonic_read` returns raw timer ticks and no syscall reports their rate.

This milestone is a board-neutral MAVLink facility: two contracts, a 16550 transmit
driver, a producer that paces itself off the clock authority, one composition, and the one
syscall the cadence needs. It names no SoC, no pin, and no clock rate, and it is the second
consumer of the declared-device mechanism `IO8` introduces.

## Exit conditions

Under QEMU: the `sel4-mavlink` composition admits; the driver binds no device and stays resident
answering the protocol's no-device status; the heartbeat producer holds its 1 Hz cadence and
reports each refused send; graph health stays green; and the plane's boot layout matches its
frozen fixture. On the host: the generated Rust encoder and the checkers' Python decoder
reproduce the same pinned frame vectors byte for byte, and the new clock syscall is covered by
root unit tests.

Host tests and a QEMU plane do not establish anything about a physical UART..

## Closure

Closed 2026-09-15 on the observed exit. `just sel4_mavlink_graph_check` booted the
`sel4-mavlink` composition (generation 56) on QEMU: the root admitted eight
executables, init launched `uart16550-driver` at slot 10 and `mavlink-heartbeat` at
slot 11 before Slisp, the root installed the driver's declared quota with no device
behind it, the driver printed `device absent, refusing requests` and stayed resident,
the producer sent `seq=0` through `seq=5` at deadlines exactly one second of the
root's reported counter rate apart, each refused `no-device`, the supervisor
certified `required=6 live=6 idle=6 failed=0`, and the boot layout matched its blessed
fixture (8 slots). On the host, `components/proto/tests/mavlink_heartbeat.rs` and the
checker's heartbeat-vector case reproduce the contract's three pinned frames from Rust
and from Python, `components/proto/tests/serial_device.rs` covers the transmit
protocol, and `components/lib/src/uart16550.rs` covers the register sequence. The clock
rate syscall the Context calls missing, `clock RATE_READ`, was already in the tree with
its root unit tests, so no root change was needed. Entry:
`devlog/2026-09-15-io9-serial-tx/`. The line behind the driver is a platform's,
supplied in `components/services/uart16550-driver/src/line.rs`; this tree carries none.
