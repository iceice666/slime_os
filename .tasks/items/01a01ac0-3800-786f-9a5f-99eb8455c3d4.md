---
schema: work-item/v1
id: 01a01ac0-3800-786f-9a5f-99eb8455c3d4
key: B76
kind: bug
state: done
created: 2026-08-20T00:00:00+08:00
closed: 2026-08-20T00:00:00+08:00
tags:
  - backlog
---

# `IpcError::PeerDead` is declared and status-mapped but never constructed, so three brokers carry unreachable death-detection arms

## Context

Resolved 2026-08-20. **Class:** Defect (a status the transport can never produce, read as working redundancy behind two real detection gaps).

`IpcError::PeerDead` had zero constructors and 43 unreachable consuming arms across 14 files; `call_broker` inferred its clock's death from the *server's* unrelated supervision handle, and `operation_broker`'s `time_closed` was a write-only self-latch nothing else read.

Class: Defect (a status the transport can never produce, read as working redundancy behind two real detection gaps).

## Exit conditions

the variant and every unreachable arm were removed; the call-plane clock was granted its own supervision handle (`fabric-call-time-supervision`, slot 9, three fixtures) and the broker's exit predicate now closes on it directly, with a park-condition fix (found by review) preventing the broker from blocking on a wake no remaining peer could signal; the operation plane's lack of clock supervision was recorded as a deliberate decision rather than left implicit. `just sel4_call_check`, `just sel4_operation_check`, and `just sel4_fabric_aggregate_check` (279 byte-identical trace records) all pass.

## Verification

Gates: `just sel4_call_check`, `just sel4_fabric_aggregate_check`, `just sel4_operation_check`

## Evidence

[`devlog/2026-08-20-b76-peer-death-cleanup/`](../devlog/2026-08-20-b76-peer-death-cleanup/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 240: `B76 — `IpcError::PeerDead` is declared and status-mapped but never constructed, so three brokers carry unreachable death-detection arms`.
