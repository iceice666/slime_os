---
schema: work-item/v2
id: 01a0d402-b9c1-7e07-a55f-6aaa8c37d778
kind: bug
state: done
created: 2026-09-24T23:22:23+08:00
updated: 2026-09-25T11:38:37+08:00
closed: 2026-09-25T11:38:37+08:00
tags:
  - backlog
  - memory-capacity
  - core-runtime
related:
  - 01a09b65-4386-7b79-a64a-f263de037b8d
---

# Private-memory ledger refuses a legal successful-cleanup abort, leaving the transaction pending and wedging all adaptive growth

## Context

Found by the 2026-09-24 post-closure stress audit of MEM-ADAPTIVE (ledger
harness `build/mem-adaptive-stress2/ledger/`, `tests/repro.rs`
`f1_abort_table_bound_wedges_pending`; randomized state machine: 6714 refusals
of physically legal retention in 100k seeds x 200 steps).

`boot-contracts/src/private_memory_policy/ledger.rs` `Ledger::abort` with
`cleanup_succeeded` bounds retained leaf tables by
`old.pages + pending.pages`. Real tables are one per 2 MiB span touched, which
is not bounded by committed pages: `begin(513)` (large frame + table + page in
span 1) fails and aborts retaining 1 table (Ok); `begin(1)` in span 0 fails and
aborts retaining 1 table: `Err(Cleanup)`. The pending transaction then stays
`Some`: every later `begin` by any subject and the holder's `retire` return
`Transaction`. Root `settle_failure` returns `Policy(Cleanup)` and never falls
back to `abort(ZERO, false)`, so root cannot recover.

## Exit conditions

The retained-table bound admits every retention a real span layout can
produce, and root never leaves a transaction pending after any abort outcome.
Ledger host tests reproduce the sequence above and observe `Ok`; slime-root's
failure path is covered.

## Verification

Gates: `just test_host`, `just test_sel4_root`, `just private_memory_phase4_check`

## Closure evidence

Fix commit `6922f71`. `Ledger::abort` no longer bounds retained tables by payload pages: retention past the committed frontier is bounded by the tables the transaction priced, and root's `settle_abort` closes a refused settlement with `abort(.., ZERO, false)`, so the transaction never stays pending. Regression tests in `boot-contracts/src/private_memory_policy/tests.rs`: `rollback_retains_one_table_per_touched_span_not_per_page` (the begin(513)/begin(1) repro from the stress harness) and `a_refused_settlement_never_leaves_a_transaction_pending`. Observed: after two failed growths each retaining one table, `begin` and `retire` for other members succeed and ledger free bytes match the physical inventory.

Branch `fix/mem-adaptive-stress-audit` (parent `9fee40d`). `just
private_memory_phase5_check` passed at `5583149` with exit 0
(`build/phase5-fix3to10.log`, sha256
`95ed582823f35fad2a92258cde44742b1bd2daab0e675e88f31683cf47f6b7cd`, QEMU
11.0.1): all six matrix rows (arm 1/2/4 GiB, rv64 1/3/4 GiB), the adaptive
workloads on both architectures, adaptive-lifecycle, and the elastic,
fragmentation, rollback, conservation, cspace, metadata, bootstrap, capacity
and stress arms, plus `test_sel4_root` 329/329 and `sel4_gate_control_check`
(45 gates plus 9 adaptive arms rejecting 2763 mutated transcripts).
`just test_host`, `just fmt_check_all`, `just lint_all` and `just docs_check`
passed. These are QEMU observations only; no hardware claim.
