---
schema: work-item/v2
id: 01a0aa6c-bba5-76d0-8ae1-cdc7d3b96253
kind: task
state: done
created: 2026-09-16T21:34:07+08:00
updated: 2026-09-18T09:24:59+08:00
closed: 2026-09-18T09:24:59+08:00
tags:
  - github
---

# Automate retirement PRs for completed MyQue work items

## Problem

The pinned MyQue 0.2.0.0 supports explicit retirement but does not retire an
item when it closes. Future completed items therefore need a repeatable
maintenance workflow that preserves exact history and proposes a reviewable
storage transition without deciding whether the work was completed correctly.

Retirement is independent of envelope migration: ordinary work-item/v1 and
work-item/v2 items may both retire without changing their envelope version.
MyQue owns identity, storage transactions, retained snapshots, and recovery;
this repository owns candidate selection, evidence policy, and publication.

## Scope

- Add a separate `.github/workflows/myque-retire.yml`, scheduled daily and
  manually dispatchable. Manual dispatch defaults to preview, with an explicit
  option to publish a PR. Read only canonical `main` at a recorded full commit;
  do not accept arbitrary source refs or commands.
- Select only live items with state `done`; exclude cancelled, unfinished, and
  already retired items. An empty candidate set succeeds without publishing a
  branch or PR. Do not impose an additional age threshold.
- Use an isolated checkout, the pinned tools, complete repository history, and
  explicit fetches of `refs/myque/retained/*`. Serialize runs with a dedicated
  concurrency group and do not cancel a run during publication.
- For ordinary prose items, document one unambiguous `## Closure evidence`
  section containing observed results and limits. Pass its content verbatim as
  retirement evidence, without summarizing or truncating it. Missing, empty, or
  ambiguous sections cause a reported skip. Do not interpret an Exit conditions
  section as proof that those conditions were observed.
- For devloop items, reference recorded evidence identities and results rather
  than rerunning gates or judging historical completion against today's expiry
  time. Preserve `recordSchema`, `schema`, `bodyProfile`, `helper`,
  `requirementsDigest`, and `admission` in the retained consumer entry, using
  the pinned consumer contract and existing adapter. The full body, epoch, and
  evidence remain in exact MyQue history. Report and skip missing required
  records, unsupported versions, and unknown consumer namespaces; do not silently
  discard consumer data or infer evidence.
- Run `just tasks_check` before and after retirement. The pre-retirement check
  must validate live spec-driven bodies, which the terminal-only check cannot
  validate. Invoke MyQue for every canonical storage mutation; do not synthesize
  terminal records or a second persistent queue or manifest.
- Verify exact retained bytes and limit the resulting diff to deletion of each
  selected Markdown file and addition of its same-UUID terminal record. Preserve
  envelope version, UUID, title, lifecycle timestamps, state, and relationships.
- Push and verify this batch's retained refs before publishing the maintenance
  branch or PR. A failed history push prevents PR publication. Never remove
  retained history automatically to undo a partial publication.
- Keep at most one open retirement PR. An existing one stops a new batch rather
  than rewriting a branch under review. After an interrupted publication,
  inspect and verify an existing bot branch or PR before resuming; do not
  overwrite unexpected commits or create duplicate PRs.
- Include source revision, UUIDs, titles, evidence sources, retained commits,
  verification, and skip reasons in the PR or linked run report. Require normal
  CI and human merge; do not write directly to `main` or enable automatic merge.
- Protect against a candidate being edited or reopened on `main` while its PR
  is pending. Verify repository merge rules require current-base checks, or
  establish an equivalent enforced freshness boundary before enabling scheduled
  publication. Workflow concurrency alone does not protect against branch edits.
- Use `GITHUB_TOKEN` with only required contents and pull-request write access.
  Account for approval requirements on CI triggered by token-created PRs; do not
  claim unattended CI without observing it. Keep privileged PR events and item
  content out of the executable control path. Leave the existing projection
  workflow's canonical-store read boundary unchanged.

## Non-goals

- Closing items, proving past completion, synthesizing observations, or bypassing
  devloop completion. Direct `myque close` can bypass devloop eligibility, so a
  done state alone does not prove that `devloop complete` was used.
- Bulk v1-to-v2 migration, cancelled-item retirement, or backfilling historical
  closure evidence to make old prose automatically eligible.
- Shrinking Git history, pruning retained refs, automatic merge, or adding a
  personal access token or GitHub App as a prerequisite.
- Replacing MyQue storage or devloop semantics, adding a new schema authority,
  or using the GitHub projection as canonical state.

## Expected implementation surface

- `.github/workflows/myque-retire.yml`: triggering, permissions, isolation,
  concurrency, remote history publication, branch creation, and PR publication.
- `scripts/lib/work_item_retirement.py`: preview/apply entry points, candidate
  and evidence policy, MyQue invocation, and exact-result verification.
- `scripts/lib/devloop.py`: consumer retention handling through the existing
  adapter rather than a parallel consumer implementation.
- `scripts/check/check-work-items.py`: extend the existing control mechanism for
  retirement policy and failure cases; no new top-level checker is needed.
- `docs/getting-started/06-work-item-lifecycle.md` and `CONTRIBUTING.md`: closure
  evidence, automated maintenance PRs, retained-ref transport, and recovery.

Reuse existing pinned tool environments where suitable. Any additional build
configuration must be justified by the actual execution requirements, not by
introducing another copy of the toolchain pins.

## Exit conditions

1. In disposable stores, done v1 and v2 items retire without migration; open,
   active, blocked, deferred, cancelled, and already retired items are unchanged.
   Preview changes neither the canonical store nor remote refs, branches, or PRs.
2. Empty runs and retries do not create duplicate branches or PRs. An existing
   open retirement PR is not rewritten. Interrupted publication resumes only
   after verifying that existing remote state is the expected bot-owned result.
3. Missing or ambiguous prose evidence, missing devloop retention information,
   unknown consumer namespaces, and unsupported versions are reported without
   retiring those items or fabricating evidence. Corrupt stores fail before
   publication rather than being treated as eligible input.
4. Before/after checks pass; UUID resolution, dependency readiness, state,
   timestamps, and relationships are unchanged. Full item bytes and consumer
   entries are verified in retained snapshots, and reopening in a disposable
   repository restores the same UUID and original body.
5. A local bare-remote experiment demonstrates retained-ref push failure blocks
   branch/PR publication, partial publication can be retried safely, and a fresh
   clone with explicitly fetched retained refs can recover retired content.
6. A candidate edited or reopened on `main` cannot be retired by merging stale
   results. Observe the required merge/check behavior, including the race after
   planning; a local pre-push comparison alone is insufficient.
7. Observe a real GitHub manual run, maintenance PR, restricted diff, retained
   refs, and the actual CI triggering/approval path before enabling the daily
   schedule. Confirm no direct `main` write or automatic merge is possible
   through this workflow's supported path. Record the tested revisions and
   remote identities; local simulations do not satisfy this obligation.
8. Run the applicable `just tasks_check`, `just docs_check`, `just ruff`, and
   `just typos` checks, plus workflow syntax validation. Record actual results
   and limits in this item's closure evidence, not merely implementation status.

## Delivery boundary

This is an ordinary prose planning item, not an admitted devloop specification.
Land this item in a work-item-only planning PR before starting implementation.
Keep it open until the exit conditions have been observed. Any missing remote
permissions or merge-rule prerequisite must be reported explicitly, not replaced
by a weaker local-only completion claim.

## Implementation evidence and remaining qualification

Observed locally on 2026-09-16, based on canonical main
`61020f02f223b977b4c565b1ad3b9efc635b575d`:

- `just tasks_check` passed with 307 items and zero live items subject to devloop
  validation. The new disposable-store controls executed v1/v2 retirement,
  evidence refusals, unchanged graph/readiness, exact history, tampered terminal
  records, stale candidates, retained-ref push refusal/retry, and fresh-clone
  recovery against a real local bare Git remote.
- The preview CLI read that canonical main revision and reported zero eligible
  items and zero skips, without publishing a branch, PR, or retained ref.
- A separate disposable-store smoke used the exact retained bytes of the real
  devloop item `01a0a828-a08b-77f1-955e-4c7e996bdbdd`, retired and verified it
  end to end, preserved failed/expired evidence as history, and refused missing
  evidence, an unknown profile/mode, and a malformed result type. This does not
  requalify the original item's completion.
- `just docs_check`, `just ruff`, `just typos`, Python formatting checks, and
  actionlint on both changed workflows passed. No OS behavior or image changed;
  no QEMU or physical-machine tests were run.

Independent review findings were corrected: hidden HTML/example evidence is
refused, thematic breaks after list evidence no longer truncate it, and private
ruleset bypass inspection is performed by an administrator rather than requiring
unavailable permissions from the workflow token. New controls cover all three.
The administrator's read-only `--approve-rules` command must supply
`MYQUE_RETIRE_RULES_APPROVAL`, bound to ruleset IDs and update timestamps;
workflow publication refuses absent/stale approval. Required checks, actionlint,
Python formatting, and the repository Nix formatter passed after these fixes.

Remote prerequisites are not satisfied. The active main ruleset requires PRs
and has no bypass actors, but has no required status checks. The Actions
create/approve-PR setting is disabled. The real publisher protection preflight
refused this configuration. Automation has not changed repository settings.
The daily activation variable is absent, and the schedule remains inactive.

Exit conditions 6 and 7 still require operator-approved repository settings,
the implementation workflow on canonical main, and an observed real GitHub
manual publication, CI/approval path, and stale-base refusal. Local controls do
not satisfy those remote obligations. This item is not complete and must not
be closed or automatically retired on these local results alone.

## Remote qualification evidence

The preceding "Remote prerequisites are not satisfied" paragraph describes the
2026-09-16 state and is superseded by the observations below, made on
2026-09-17 and 2026-09-18 against real GitHub runs, PRs, refs, and merges.

### Repository configuration observed

Ruleset `23434247` (`main`, active) has empty bypass actors and requires pull
requests, deletion/non-fast-forward protection, linear history, and the status
checks `Canonical work-item store` and `CI` from GitHub Actions integration
`15368`, with `strict_required_status_checks_policy: true`. Repository
`allow_auto_merge` is `false`, and the workflow performs no merge. Actions can
create pull requests; both maintenance PRs were opened by `app/github-actions`.
`MYQUE_RETIRE_RULES_APPROVAL` is
`42c3a8cc37be7291b35f17537dd7b1c0adb35c8337f5a9065a879a02a7e92d92`.

### Publisher defect found and fixed during qualification

The first real `publish=true` run,
`https://github.com/iceice666/slime_os/actions/runs/35241965762` on source
`b2e032dfaa0b32a3ca2fec69f599cf0333782a03`, refused with `ruleset approval
missing or stale; an administrator must run --approve-rules`. It created no
branch, no PR, and no retained ref; remote retained refs stayed at 248. The
cause was token-dependent ruleset `updated_at` serialization: the administrator
token returned `2026-09-17T23:14:28.832+08:00` while the Actions token returned
the equivalent `2026-09-17T15:14:28.832Z`, and the digest hashed the raw string.
PR #109 normalizes the timestamp to UTC before hashing and adds controls for
timezone-equivalent, timezone-naive, and malformed values. That safe refusal is
itself evidence that the preflight blocks publication before any remote effect.

### First real publication

Run `https://github.com/iceice666/slime_os/actions/runs/35245916898` published
from source `bfe9a35be47ea9112d349b0a61c05c389091abf9` with 5 eligible items and
0 skips, retirement commit `e4c27c448edf3ff02e1ea261b691722751bfc505`, branch
`automation/myque-retire/bfe9a35be47ea9112d349b0a61c05c389091abf9`, and PR #110.
Remote retained refs went from 248 to 253. The diff was storage-only: five
`.tasks/items/*.md` deletions and five same-UUID `.tasks/terminal/*.json`
additions, 5 insertions and 916 deletions.

Its CI run `35246697046` was created as `action_required` with no check runs on
the head SHA, confirming that a `GITHUB_TOKEN`-created PR does not imply
unattended CI. A maintainer approved that existing run through GitHub's run
approval endpoint; it then ran and passed every check, including
`Canonical work-item store` with its trusted retirement verifier.

### Stale-base race, observed on GitHub

With PR #110 already planned, candidate `01a0aef9-4c68-7251-8b66-291fe9f8cf4f`
was edited on canonical `main` through `myque api put --expected` and landed by
ordinary PR #111, merged as `d49bd981c68207ef7c730550b2afb5bdc4e412f6`. GitHub
then reported for the still-open PR #110: `mergeable: false`,
`mergeable_state: dirty`, `rebaseable: false`, GraphQL `mergeable: CONFLICTING`,
`mergeStateStatus: DIRTY`, and a `diverged` comparison one commit ahead and one
behind merge base `bfe9a35be47ea9112d349b0a61c05c389091abf9`. Its earlier green
checks remained bound to the old head/base pair and did not make it mergeable.
PR #110 was closed unmerged, with no rebase, force-push, or branch rewrite, and
its retained refs were preserved.

### Recovery boundary after a closed batch

Re-dispatching immediately produced run
`https://github.com/iceice666/slime_os/actions/runs/35291031278`, which refused
with `retirement PR was closed without merging; review before retrying` and
created no branch, PR, or ref. After the closed PR and its bot-owned branch were
reviewed, an operator deleted only that stale branch; retained refs stayed at
253 and PR #110's record was preserved.

### Qualifying publication and merge

Run `https://github.com/iceice666/slime_os/actions/runs/35291631752` published
from `d49bd981c68207ef7c730550b2afb5bdc4e412f6` with 5 eligible items and 0
skips, retirement commit `1f3833b501976ad19c96da56893ad167bfc899b3` and PR #112.
Retained refs went from 253 to 258. Retained commits were
`5ad86b7099afe86fbb8da1e1d4f007668c4eba18`,
`5eaa39f8758e67ea737af85468c4cbb54a3675e5`,
`bb4dc9ac8220542885c7ca411ec15a2524f5fb39`,
`164ab3f91f8183f5be0b01cf8362b27683243158`, and
`e5cc9b0b8c142df6eff26edd7c2d726daf308a64`. The retained snapshot of the edited
candidate matched the updated canonical bytes exactly, blob
`dd38baa91d61d28454cb779802235c33313e3c70`, so the fresh batch used current
content rather than the stale body.

Its CI run `35292091447` was again created as `action_required`, was approved
through the supported GitHub path, and then passed every check, including
`Canonical work-item store` running `just tasks_check` plus the trusted
retirement verifier against current `main`. A human merged PR #112 as
`8f8572cf4c807e97fbce6df3da7d92585c25b085`.

### Recovery and post-merge verification

A fresh clone, holding no retained refs from the qualification host, checked out
`1f3833b501976ad19c96da56893ad167bfc899b3`, explicitly fetched
`refs/myque/retained/*` to 258 refs, and ran `myque reopen` on all five terminal
records. Each restored the same UUID with byte-identical body, title, and
`work-item/v2` schema compared with source
`d49bd981c68207ef7c730550b2afb5bdc4e412f6`, and `myque check` passed. An earlier
fresh clone likewise verified the first batch's five retained blobs against
`bfe9a35be47ea9112d349b0a61c05c389091abf9`.

On merged `main` `8f8572cf4c807e97fbce6df3da7d92585c25b085`, the five live
Markdown files are gone, the five terminal records are present with their
`terminal-item/v1` schema, original paths, retained commits, and digests, the
remote still holds 258 retained refs, and `just tasks_check` passed with
314 items and 1 validated through devloop.

### Limits and what remains unproven

`MYQUE_RETIRE_ENABLED` is still unset, so the daily schedule remains inactive;
it may be enabled only after this closure lands. Qualification used
`workflow_dispatch` only, so scheduled triggering is not yet observed. CI on a
token-created PR required maintainer approval in both real batches, so
unattended CI is not claimed. A candidate whose bytes change on `main` is
refused by a merge conflict on the storage-only diff, which is what was
observed; the strict required checks were configured but the conflict alone
blocked the stale merge. A direct `myque close` can still bypass devloop
eligibility, and no OS behavior, image, QEMU, or physical-machine claim follows
from this storage-maintenance work.
