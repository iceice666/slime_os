---
schema: work-item/v1
id: 01a0a06d-61e3-7b94-b8b9-d6228201b139
kind: bug
state: done
created: 2026-09-14T14:58:38+00:00
updated: 2026-09-14T16:03:45+00:00
closed: 2026-09-14T16:03:45+00:00
tags:
  - backlog
---

# Two source gates refuse the tree after #61: run records and the closure aggregate

## Context

`scripts/generate/generate-system-test-runs.py` derives a run record for every
`scripts/check/check-sel4-*.py` and refuses one that boots no `build/slime-*.elf`
image unless `NOT_A_PLANE` lists it. `0361bff4` (#61) added
`check-sel4-x86-64-image.py`, an admission check over built pc99 artifacts that
boots nothing, without listing it, so `system_test_run_check`,
`system_test_run_bless`, and `contracts_check` stop at
`check-sel4-x86-64-image.py: boots no build/slime-*.elf image` before comparing
any record. `check-system-image-aggregate.py` learns what a checker boots by
scanning `scripts/check/` alone; #61 moved image-name composition into
`scripts/lib/sel4_boot.py`, so the product root-boot RV64 image became
invisible and its exemption "stale", and the pc99 images, knob, and dual-path
gates #61 added were unclassified behind that.

## Exit conditions

`just system_test_run_check`, `just system_image_closure_aggregate_check`, and
`just contracts_check` pass on a tree carrying #61, the generator still refuses
an unlisted checker that names no image, and the aggregate gate's own mutation
controls still reject an invented knob and an unlisted dual-path gate.

## Verification

Gates: `just system_test_run_check`, `just system_image_closure_aggregate_check`,
`just contracts_check`. Entry:
`devlog/2026-09-14-source-gates-after-61/`.

## Closure

Closed 2026-09-14 on the observed exit: on the IO8 branch carrying #61,
`just system_test_run_check`, `just system_image_closure_aggregate_check`
(23 booted plane images, 10 mapped and 13 exempt with a reason, all 55 closures
exercised, its own mutation controls rejecting an invented knob and an unlisted
dual-path gate), and `just contracts_check` pass, and the baseline's generator
run in place against the same checker set still stops at
`check-sel4-x86-64-image.py: boots no build/slime-*.elf image`. Entry:
`devlog/2026-09-14-source-gates-after-61/`.
