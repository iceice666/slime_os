---
schema: work-item/v1
id: 019fe740-a000-722b-bf6a-29f2db805351
key: B47
kind: bug
state: done
created: 2026-08-10T00:00:00+08:00
closed: 2026-08-10T00:00:00+08:00
tags:
  - backlog
---

# package, process, thread, service instance, and lifecycle are one Task model

## Context

Resolved 2026-08-10.

one `Task` meant image instance, CSpace/VSpace owner, single TCB, service identity, scheduling unit, and lifecycle identity at once, forcing every component to be single-threaded.

## Exit conditions

a process runs up to `MAX_CHILD_THREADS` threads sharing one CSpace/VSpace, each with its own TCB, stack, IPC buffer, transfer window, and schedule, with per-thread TLS identity set in the register context rather than through a kernel call a later `WriteRegisters` would overwrite; `sample-worker` declares an extra thread and both threads are observed printing, with two mutations (never resuming the worker, aliasing thread indices) confirmed to fail. `just test_sel4_root` (146), `just sel4_spawn_check`, `just sel4_supervision_check`, `just sel4_reclamation_check`, `just sel4_boot_check`, and the full 31-plane sweep pass.

## Verification

Gates: `just sel4_boot_check`, `just sel4_spawn_check`, `just sel4_supervision_check`, `just test_sel4_root`

## Evidence

[`devlog/2026-08-10-b47-runtime-threads/`](../devlog/2026-08-10-b47-runtime-threads/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 676: `B47 — package, process, thread, service instance, and lifecycle are one Task model`.
