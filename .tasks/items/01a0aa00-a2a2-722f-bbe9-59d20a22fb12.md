---
schema: work-item/v2
id: 01a0aa00-a2a2-722f-bbe9-59d20a22fb12
kind: task
state: blocked
created: 2026-09-16T19:36:03+08:00
updated: 2026-09-16T20:42:31+08:00
tags:
  - build
  - image-size
---

# Reduce final seL4 image size through ELF packaging and measured codegen

## Problem

Reduce final seL4 image bytes without reducing memory capacity, removing boot
checks, weakening ELF admission, or changing the capability model. The requested
scope is four optimizations: embedded component ELF stripping, measured root
codegen, embedded child ELF stripping, and unnecessary unwind metadata removal.

Earlier measurements used existing build outputs, not an identity-bound rebuild
of one revision. They identify candidates, not verified savings. A smaller ELF
file does not imply fewer mapped pages, less RAM, or fewer root CSlots: report
file bytes and loadable memory separately, including BSS, alignment and stacks.

## Scope

1. Strip non-runtime metadata from embedded component ELF delivery copies before
   generation encoding and signing. Preserve the original symbol-bearing build
   artifacts. Reuse the existing ELF tooling/admission path; retain entry points,
   required program headers, segment permissions, load bytes and zero-fill
   semantics. Cover workspace and external components without silently weakening
   input admission or changing the meaning of external source identities.
2. Measure root size-first codegen and supported LTO configurations against a
   rebuilt baseline. Compare the final packaged image, not just object files.
   Keep only configurations with observed size benefit and passing runtime gates.
   Scope changes to the intended build path; Cargo does not support LTO in a
   package-specific profile override. Do not impose unmeasured workspace-wide
   profile changes or assume size optimization always produces a smaller image.
3. Strip the embedded native child ELF delivery copy while retaining its symbols
   separately. Preserve all required fixture, fault, IPC and reclamation checks.
   Remove a fixture from a plane only if its actual control flow proves it unused;
   fixture removal is not an excuse to narrow existing verification.
4. Resolve actual panic, unwind, backtrace and target requirements before removing
   root unwind metadata. Prefer a build/link change over post-link corruption of
   program headers. A construction-failure path named unwind is not evidence of
   Rust stack unwinding. Demonstrate that supported boot and fault behavior remains
   intact; document any metadata that must remain and its measured cost.

## Boundaries

- Do not change descriptor capacities, allocator representation, CSpace limits,
  private-memory budgets, MEM-1G qualification or adaptive memory design.
- Do not strip the packaged loader ELF blindly: it carries sectionless payload
  segments. Kernel/root packaging and embedded component stripping are distinct.
- Do not promise a fixed KiB reduction before building comparable artifacts.
- Do not bless changed boot-layout fixtures solely because a check fails. Review
  the semantic change and preserve capability-slot contracts.
- Source changes require regenerated system-image closures and exact tested image
  identities. No physical-board qualification follows from QEMU execution.

## Exit conditions

- Capture before/after source revision, target, build settings, image identity and
  exact final bytes. Separately report generation bytes, root/child ELF file size,
  loadable file and memory extents, and removed symbol/unwind metadata. Do not add
  overlapping per-step savings or equate file-size savings with CSlot savings.
- Repeated equivalent builds produce identical delivery bytes. Stripped embedded
  ELF copies pass existing host/root admission; malformed inputs remain refused.
  Demonstrate preservation of the entry point, permissions, loadable bytes and
  zero-fill behavior, not merely that a stripping command exits successfully.
- Boot rebuilt root and product-component paths on AArch64, RV64 and x86-64 QEMU
  wherever the shared build changes apply. Exercise the child fixture and actual
  fault/reclamation paths. Run the existing fault-plane gate as well, but do not
  treat that alone as proof that an unwinder is unnecessary.
- Run `just contracts_check`, `just generation_check`,
  `just system_image_builder_check`, `just sel4_root_boot_check`,
  `just sel4_fault_check`, `just sel4_boot_layout_check`, and
  `just x86_64_sel4_root_boot_check`; use the existing root/component-graph
  checkers' platform arguments for RV64 and other affected product lanes.
- Run `just fmt_check_all` and `just lint_all` for permanent Rust changes, and
  `just ruff` for Python changes. Extend existing checks only for observable
  regressions; do not add a new top-level checker for this optimization alone.
- Update owning build documentation where procedures or symbol-artifact locations
  change. Record measured savings, selected/rejected codegen configurations,
  verification commands and retained limitations before closing this item.

## Status

The four implementation changes and reachable local checks are complete. Closure
is blocked on the required pinned x86-64 Linux runtime qualification below; this
item is not done and no pc99 boot pass is claimed.

## Observed implementation evidence (2026-09-16)

Baseline rebuilt from `3352193006c2e916698f3b54675ec32fe6758894`, target
`aarch64-sel4-qemu-virt`, fixture variant, pinned `nightly-2026-04-04`,
QEMU 11.0.1 with 2048 MiB. Baseline and selected configuration both passed the
ordered root boot, child IPC, fault, private-memory and reclamation checks.
Selected artifacts include this item's uncommitted implementation changes.

| Artifact / measurement | Baseline bytes | Selected bytes |
|---|---:|---:|
| Packaged loader image | 5954136 | 5634528 |
| Generation | 424029 | 262669 |
| Root ELF file, with symbols | 5786632 | 5402176 |
| Root summed PT_LOAD filesz | 5646172 | 5326564 |
| Root summed PT_LOAD memsz | 7401564 | 7082012 |
| Embedded child ELF | 41016 | 21384 |

The final image reduction is 319608 bytes (312.12 KiB, 5.37%). Segment sums are
not page-rounded footprint or CSlot counts. No descriptor or memory-capacity
limits changed. The original symbol-bearing child remains 41016 bytes.

- Baseline image SHA-256:
  `4b18bd64f98c6e2dd67fd7c9cffffdca60f2dac318d00417f5346b5c91dfa474`.
- Selected image SHA-256:
  `578074a6cd3662a247365714e33f60cd1f1b2c31a964f893c425216dde3ac414`.
- Selected root SHA-256:
  `752da6a62c8f3c7f0d773ee1db99035dfb23f9969a69f8310dc5d82276a6b8d8`.
- Selected generation SHA-256:
  `87d660206fa86fc8dedf1b992b6a7a7720a8d02a35c9492e86919bfd769dc9de`.

With the baseline generation and child held fixed, root-only codegen produced
these packaged image sizes: s/CGU1/local LTO 5895856, z/CGU1/local LTO 5904184,
s/fat LTO 5832376, z/fat LTO 5824104, s/fat/no-unwind 5815416,
z/fat/no-unwind 5797328 bytes. The selected shared profile is s/CGU1/fat LTO
with root-only `force-unwind-tables=no`. Although z was smaller on ARM, its
RV64 root fixture failed during fault-settlement output with a cap fault;
changing only the optimization level to s passed that required path. The
failure's root cause is not established; no platform exception or runtime
workaround was introduced.

The baseline root's `.eh_frame_hdr` and `.eh_frame` occupied 4380 and 23172
bytes respectively; neither section exists in the selected ARM root. The pinned
runtime guards its unwinder with `cfg(panic = "unwind")`, while this build uses
abort. The construction-rollback path and required child fixture remain enabled.

Repeated real stripping of ARM child, init and external Slisp produced identical
delivery bytes without modifying input files. RV64 stripping exposed LLVM moving
the nonloaded attributes segment; the verifier now admits only equivalent metadata
relocation and regression cases reject changed instructions, entry, LOAD headers,
attributes and malformed original input.

### Verification limitations

`just x86_64_sel4_root_boot_check` was attempted. A relocated local CMake cache
was preserved and replaced with a fresh build. The fresh pc99 kernel then failed
the pinned digest check: observed
`16d52247858fc263d6f7ebc42c2c876b92129aa70b5d4f0a090dbec463909d53`, expected
`1ae6fd038c8f24ba9a1be0ae53396a9cae881c620b4c15e96294b0e48b50f5e4`.
This Darwin/ARM shell also lacks the pinned x86-64 Linux GRUB/OVMF inputs.
No pins were changed or bypassed to claim a boot pass. A pinned x86-64 Linux
runner remains necessary for the required pc99 runtime qualification.

An additional pc99 userspace-only smoke built and stripped the native child
(31680 to 18928 bytes, delivery-equivalence check passed), then stopped at the
external C component link: this Darwin host selected ld64.lld, which rejects the
ELF linker arguments. This is not a pc99 root/component runtime pass.

The existing sample/component-graph checkers' non-pc99 build branch always selects
an AArch64 closure, even with `--platform qemu-riscv-virt`. Do not count that as
RV64 evidence. The RV64 component smoke instead builds the existing sample-plane
variant for RV64 explicitly, verifies its identity, and calls the existing
sample checker's boot/transcript validation on that exact image.

The explicit RV64 sample smoke first exposed `MissingWorkerImage`: unlike basic
entry admission, `ChildImage::worker()` reads the worker entry and stack from
the symbol table. The strip command now retains exactly those two runtime ABI
symbols. After that source fix, the same RV64 sample path passed all 17 ordered
markers with s/fat LTO. A real llvm-strip regression additionally checks retained
worker values/sizes and removal of an unrelated symbol. Removing these required
symbols is not an image-size optimization.

The existing closure cache keys only on declared input identities. Generation
builder, image builder and ELF delivery helper are now explicit release inputs,
and missing/changed delivery-input regression cases exercise resolution refusal.
Closures were regenerated so an earlier stripped-worker image cannot be reused
under the final source state.

The successful RV64 sample image is 5551680 bytes, SHA-256
`e93cd79147d323a5cea5de33737cab8f225a282cf800ec8c21b842753b44f1a1`.
The selected ARM fixture image, root and generation also reproduced their exact
recorded SHA-256 values on a second build. Retaining the worker ABI symbols does
not change that fixture's measured image because its embedded components do not
declare that worker pair.

### Completed local checks

- `just sel4_root_boot_check`: passed with the final symbol-preserving delivery
  path and s/fat/no-unwind root profile.
- `python3 scripts/check/check-sel4-root-boot.py --platform qemu-riscv-virt`:
  passed with s/fat/no-unwind and stripped native child.
- Platform-correct RV64 sample smoke: 17 ordered markers passed, including the
  worker thread whose runtime symbols must survive stripping.
- `python3 scripts/check/check-architecture-contract.py`: passed, including
  real pinned-tool worker-symbol preservation and malformed-source controls.
- `just test_sel4_root`: 243/243 tests across 21 modules passed.
- `just fmt_check_all`, `just lint_all`, `just ruff`, `just docs_check`,
  `just tasks_check`, and `just typos`: passed locally.
- `python3 scripts/check/check-system-image-closure.py`: passed resolution,
  malformed-input controls, isolated repeated builds and byte identity.
- `just system_test_run_check`: 52 records validated; 47 closure-bound records,
  5 declared non-closure records, and 5 refusal controls passed.
- Final `just sel4_fault_check`: passed bounded fault/degradation behavior with
  19 participants under the final closure identity.
- Final `just sel4_reclamation_check`: passed segmented extent and root CSlot
  reclamation/reuse, including construction rollback.
- Final `just generation_check` (including `just contracts_check`): passed
  contract validation, product graph boot and generation determinism.
- Final `just system_image_builder_check`: 54 closures resolved; the selected
  closure built twice byte-identically and non-empty output was refused.
- Final `just sel4_boot_layout_check`: all 31 plane layouts matched their
  unchanged capability-slot fixtures. No boot-layout blessing was needed.

Dependent system-test-run records were regenerated after confirming that their
only semantic change is `imageClosureIdentity` (47 records). The generator's
host-only x86 image checker exclusion was completed so identity regeneration
does not treat a non-boot artifact checker as a QEMU plane. No timeout, marker,
capability-slot fixture or execution-device expectation was blessed away.
