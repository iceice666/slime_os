---
schema: work-item/v1
id: 019fd7cd-8c00-76ca-a4f2-2f2dc6c169a0
kind: bug
state: done
created: 2026-08-07T00:00:00+08:00
closed: 2026-08-07T00:00:00+08:00
tags:
  - backlog
  - legacy
---

# `release_trust_check` was red, unregistered, and its rotation refusals never reached Rust

## Context

Resolved 2026-08-07 (B30, second use — distinct from the dango-plane defect above).

`release_trust_check` could not run at all (missing imports crashed it before any assertion), was absent from AGENTS.md's gate index so a red gate went unnoticed, and its rotation refusals tested a pure-Python reimplementation rather than the kernel's own decoder.

Legacy roadmap identifier: `B30` (second allocation). That id was allocated twice, so it is not carried as a key.

## Exit conditions

`just release_trust_check` passes, is listed in AGENTS.md's gate index, and each rotation continuity branch is guarded by its own fixture routed through `apply_rotation` itself — removing the replacement check fails with "version-skip", removing the previous check fails with "stale-previous"; a `replacement.validate()?` guard was investigated and confirmed unreachable-by-construction (covered independently by the signature-entry check), so no fixture was shipped for it and the finding is recorded rather than left as an untested gap.

## Verification

Gates: `just release_trust_check`

## Evidence

[`devlog/2026-08-07-b30-release-trust-gate/`](../devlog/2026-08-07-b30-release-trust-gate/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 1012: `B30 — `release_trust_check` was red, unregistered, and its rotation refusals never reached Rust`.
