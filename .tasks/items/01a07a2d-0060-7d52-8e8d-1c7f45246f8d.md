---
schema: work-item/v1
id: 01a07a2d-0060-7d52-8e8d-1c7f45246f8d
key: MEM-64M
kind: milestone
state: done
created: 2026-09-07T12:42:44+08:00
updated: 2026-09-13T16:12:58+08:00
closed: 2026-09-13T16:12:58+08:00
tags:
  - core-runtime
  - memory-capacity
parent: 01a07a2c-9c4f-7ca0-8417-aba2b48ce16b
depends:
  - 01a07a2d-003d-77fc-9df8-dda85ed9a083
---

# Target-qualified 64 MiB private component working sets

## Context

The first user-visible capacity target is a real 64 MiB private working set, not additional unused emulator RAM. Both QEMU architectures must carry this workload while small physical profiles retain conservative ceilings.

## Scope

- Make per-target private-region and aggregate capacity explicit in versioned Zutai-owned inputs. Update generators, component-spec quota validation in `scripts/lib/component_spec.py`, system-spec composition/admission, builder refusal, root admission, VSpace reservation/collision checks, component runtime private-heap batching, and `docs/capability-matrix.md` in one cutover. Target-neutral spec validation and target-qualified composition checks must agree without applying the QEMU ceiling to small targets.
- Set the initial QEMU qualification envelope to 64 MiB per holder and 128 MiB aggregate private working memory. Keep existing small-target limits unless separately qualified. Bind profile selection to the admitted target identity, not a caller argument or ambient environment.
- Distinguish virtual reservation, reserved physical backing, committed 4 KiB quota pages, frame-object count, and metadata overhead. Account for all resources before launch; no memory overcommit.
- Keep private memory non-transferable and non-executable. Shared buffers, ELF loading ceilings, swap, live shrink, page promotion, and hardware capacity claims are out of scope.
- Update the existing private-memory probe's fixed `MAX_PROBE_PAGES` runaway guard from the generated declared/profile bounds. Split bulk and incremental cases explicitly, including boundary and full-quota small-request evidence; size watchdogs from observed execution without removing fail-fast bounds. Carry these changes into the existing component-spec/system-spec negative fixtures.

## Exit conditions

- On AArch64 and RV64 QEMU, a declared component grows exactly 64 MiB through the public private-memory API, touching and verifying every page. A separate ordinary runtime-heap case holds 60 MiB of payload simultaneously within the 64 MiB quota; report payload, allocator overhead, and remaining capacity separately.
- A bulk exact-64-MiB private-growth case uses 32 large data frames; report extra small data frames and page tables separately for runtime-heap and incremental cases. A mixed sequence crosses 2 MiB boundaries without rounding the caller's quota or corrupting live bytes.
- Zero quota, per-holder over-declaration, aggregate over-declaration, wrong-target profile, unsupported contract version, and one-page-over-quota growth fail closed. Builder and root agree on the same declared budgets; insufficient physical backing or slots is refused before making a task runnable.
- The 128 MiB aggregate contract accepts two 64 MiB reservations where platform backing permits; existing small-profile admission still rejects a 64 MiB holder. Full concurrent 1 GiB qualification is not claimed here.
- Normal exit and deliberate task fault permit immediate reuse by a subsequent task; run 20 spawn/grow/exit or fault cycles with no downward drift in reusable memory, slots, or accounting and no stale data exposure.

## Verification

Gates to extend/run during implementation: `just contracts_check`, `just generation_check`, `just component_spec_check`, `just system_spec_check`, `just private_memory_check`, `just test_sel4_root`, `just test_host`, `just sel4_root_boot_check`, `just sel4_gate_control_check`, `just fmt_check_all`, `just lint_all`.

Use the existing private-memory probes and closure/spec pipeline, with the two-architecture execution support delivered by the prerequisite. Record exact image/profile identities and capacity evidence; compilation or a successful size query does not count as a working set.

## Recorded progress and observed exit conditions

Reopened after review: the recorded capacity results do not establish every
exit condition. The prior observations below are inherited from
[the target-qualified capacity entry](../../devlog/2026-09-13-mem-64m-target-qualified-capacity/index.md),
not a claim that the current tree passed those commands unchanged.

- `just private_memory_check` passed on **both** architectures at the declared
  16384-page (64 MiB) ceiling: `24 markers across 7 causal chains and 3 image
  case(s) on qemu-arm-virt` and `1 image case(s) on qemu-riscv-virt`, each reporting
  `the declared quota (16384 page(s)) is the measured ceiling`.
- Bulk growth used exactly 32 large data frames:
  `SLIME_MEM grown delta=16384 previous=0 pages=16384 quota=16384 large_frames=32
  base_frames=0 leaf_tables=0`.
- The five resource classes are reported separately:
  `SLIME_MEM accounting reservation=67108864 reserved=67239936 payload=67108864
  tables=131072 alignment=0 frames_large=32 frames_base=16384 descriptors=16448
  extents=65 cslots=16513`.
- The runtime-heap case held 60 MiB inside the declared ceiling with overhead and
  backing reported apart: `capacity payload=62914560 overhead=16 backed=63008768
  pages=15383 touched=1`, cross-checked against the root's declared quota.
- Both refusal arms are reached: `cause=reservation ... reservation: 16384` for the
  full-window holder and `cause=quota ... quota: 24` for the policy-bounded one.
- The 128 MiB aggregate admits two 64 MiB reservations
  (`holders=2 pages=16384 ... fit=1`), while every physical target still resolves to
  the conservative 512/2048 default — asserted by `capacity_for("aarch64-rpi5")` in
  `just contracts_check` (317 tests).
- `just test_sel4_root` 236/236, `just generation_check`, `just component_spec_check`
  (43 mutations refused), `just system_spec_check` (21 refused),
  `just sel4_reclamation_check`, `just lifecycle_restart_check`, and
  `just sel4_gate_control_check` (48 gates, 1937 mutated transcripts) all passed.

The 256 MiB/1 GiB envelope is **not** published: MEM-1G owns that raise with its own
evidence.

The raised-capacity reuse qualification is now **observed**, on its own
composition and its own gate. Recorded in
[the private reuse-cycles entry](../../devlog/2026-09-13-mem-64m-private-reuse-cycles/index.md);
the results below were produced by `just private_memory_cycles_check` on this
tree, not inherited.

- The declared 16384-page holder was reclaimed and re-served 20 times on
  `sel4-private-memory-cycles` (generation 55), on **both** QEMU
  architectures: 10 incarnations ended by clean exit, 10 by a deliberate write
  past the declared window (`[init] private memory cycles exits=10 faults=10`
  on each).
- No drift on either architecture: all twenty holder reclamation censuses
  report identical allocator watermarks —
  `slots=522045 bytes=1515227504 live_objects=145` on AArch64,
  `slots=522099 bytes=3126358976 live_objects=111` on RV64. The twenty-first
  census is init's own exit, a task that held no quota.
- Every incarnation grew from an empty region at one base, with the same
  backing shape: `SLIME_MEM grown task=N delta=16384 previous=0 pages=16384
  base=0x4000000 quota=16384 total=16384 large_frames=32 base_frames=0
  leaf_tables=0`, twenty times.
- No stale data exposure: each incarnation read every one of its 8,388,608
  words as zero before stamping each with a per-incarnation value, then read
  every stamp back (`zeroed=1 verified=1`, twenty distinct stamps). Word-level,
  not one word per page: a page-sampling sweep would leave 4088 of each page's
  4096 bytes untested.
- Terminal accounting (AArch64): `SLIME_GRAPH spawns served=20 drops=0
  terminated=21`; `mapped_ram=0 reusable_ram=68026368
  reusable_private_ram=67239936 allocation_descriptors_free=288416
  extent_reuses=1235`;
  `SLIME_GRAPH HEALTHY generation=55 required=1 live=0 completed=1 failed=0`.
- Regression suite re-run on this tree: `just private_memory_check` unchanged at
  24 markers on both architectures, `just sel4_gate_control_check` 48 gates and
  1963 mutated transcripts, `just test_sel4_root` 240/240, `just test_host` 342,
  `just contracts_check`, `just system_spec_check`,
  `just system_image_closure_aggregate_check` all passed.

One follow-up is left open and does not bear on this milestone's clause: the
root reports a component's unmapped write as `access: Execute status: 0`, the
same shape the pre-existing `reclamation-fault` probe produces, so no gate can
yet assert the access class of a deliberate write. The faulting store is inline
assembly rather than an invalid Rust dereference, so the instruction reaching
the CPU is guaranteed; only its decode is unclassified. Owner:
`slime-root/src/fault.rs`.

## Notes

Architecture and sequencing: [core runtime memory capacity](../../roadmap/02-core-runtime.md#memory-capacity). Planning decision: [memory capacity milestones](../../devlog/2026-09-07-memory-capacity-milestones/index.md). The capacity results are inherited evidence from the target-qualified entry; the reuse-cycle results were observed on this tree by `just private_memory_cycles_check`.
