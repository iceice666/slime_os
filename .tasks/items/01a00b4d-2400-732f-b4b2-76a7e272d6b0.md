---
schema: work-item/v1
id: 01a00b4d-2400-732f-b4b2-76a7e272d6b0
key: B56
kind: bug
state: done
created: 2026-08-17T00:00:00+08:00
closed: 2026-08-17T00:00:00+08:00
tags:
  - backlog
---

# `data_fabric_profile_check` asserted a contradiction and had been red since B55

## Context

Resolved 2026-08-17. **Class:** Gate defect (a check that could not pass).

`just data_fabric_profile_check` failed on the `unified` profile because the gate swept every declared profile through one resolution rule, but B55 had given `unified` a per-plane worker holder while every other profile still terminates at `fabric-service` — a manifest declaring both cannot satisfy both rules, so the gate demanded a contradiction, unobserved since B55 landed.

Class: Gate defect (a check that could not pass).

## Exit conditions

the sweep now resolves only the single-broker profiles and fails loudly if a manifest declares none of them, with `unified` left to its four boot gates for coverage. `just data_fabric_profile_check`, `just sel4_boot_check`, `sel4_traffic_check`, `sel4_fault_check`, `sel4_saturation_check`, `sel4_fabric_aggregate_check`, `just contracts_check`, `just generation_check`, `just ruff`, and `just typos` pass.

## Verification

Gates: `just contracts_check`, `just data_fabric_profile_check`, `just generation_check`, `just ruff`, `just sel4_boot_check`

## Evidence

[`devlog/2026-08-17-structural-audit/`](../devlog/2026-08-17-structural-audit/index.md)

## Notes

Migrated from `roadmap/00-backlog.md` line 547: `B56 — `data_fabric_profile_check` asserted a contradiction and had been red since B55`.
