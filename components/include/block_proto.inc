# @generated by contracts/block/v1/gen_rust.zt; do not edit.
.intel_syntax noprefix

# Source contract: contracts/block/v1/schema.zt

.equ BLOCK_FORMAT_VERSION, 1
.equ BLOCK_MAGIC, 1162562626
.equ BLOCK_OP_READ, 1
.equ BLOCK_OP_WRITE, 2
.equ BLOCK_OP_FLUSH, 3
.equ BLOCK_REQUEST_LEN, 64
.equ BLOCK_REPLY_LEN, 64

.equ OFF_REQUEST_MAGIC, 0
.equ OFF_REQUEST_VERSION, 4
.equ OFF_REQUEST_OP, 8
.equ OFF_REQUEST_FLAGS, 9
.equ OFF_REQUEST_RESERVED, 10
.equ OFF_REQUEST_LBA, 12
.equ OFF_REQUEST_SECTOR_COUNT, 20
.equ OFF_REQUEST_BUFFER_PAGES, 24
.equ OFF_REQUEST_BUFFER_PHYS, 28

.equ OFF_REPLY_MAGIC, 0
.equ OFF_REPLY_VERSION, 4
.equ OFF_REPLY_STATUS, 8
.equ OFF_REPLY_SECTORS_DONE, 12

.macro BLOCK_VALIDATE_REQUEST base, length, invalid
    cmp \length, BLOCK_REQUEST_LEN
    jb \invalid
    cmp DWORD PTR [\base + OFF_REQUEST_MAGIC], BLOCK_MAGIC
    jne \invalid
    cmp DWORD PTR [\base + OFF_REQUEST_VERSION], BLOCK_FORMAT_VERSION
    jne \invalid
.endm

.macro BLOCK_VALIDATE_REPLY base, length, invalid
    cmp \length, BLOCK_REPLY_LEN
    jb \invalid
    cmp DWORD PTR [\base + OFF_REPLY_MAGIC], BLOCK_MAGIC
    jne \invalid
    cmp DWORD PTR [\base + OFF_REPLY_VERSION], BLOCK_FORMAT_VERSION
    jne \invalid
.endm
