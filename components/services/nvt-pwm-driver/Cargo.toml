[package]
name = "slime-component-nvt-pwm-driver"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.96"
build = "build.rs"

[[bin]]
name = "nvt-pwm-driver"
path = "src/main.rs"
test = false

[dependencies]
slime-components = { path = "../../lib" }
slime-proto = { path = "../../proto" }
slime-rt = { path = "../../runtime" }

[build-dependencies]
slime-build-support = { path = "../../build-support" }

[lints]
workspace = true
