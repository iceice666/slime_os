[package]
name = "slime-component-sel4-recovery-probe"
version = "0.1.0"
edition = "2024"
publish = false
rust-version = "1.96"
build = "build.rs"

[[bin]]
name = "sel4-recovery-probe"
path = "src/main.rs"
test = false

[dependencies]
boot-contracts = { path = "../../../boot-contracts", features = ["gpt"] }
slime-components = { path = "../../lib", features = ["component-runtime"] }
slime-proto = { path = "../../proto" }
slime-rt = { path = "../../runtime", features = ["heap"] }

[build-dependencies]
slime-build-support = { path = "../../build-support" }

[lints]
workspace = true
