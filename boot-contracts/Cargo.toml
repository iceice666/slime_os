[package]
name = "boot-contracts"
version = "0.1.0"
edition = "2024"
publish = false

[features]
release-crypto = ["dep:ed25519-dalek"]

[dependencies]
ed25519-dalek = { version = "2.2.0", default-features = false, optional = true }

[lib]
doctest = false
